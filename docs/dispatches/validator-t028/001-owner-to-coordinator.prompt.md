# Freeze the independent acceptance sources during the T026 review window

Existing Sol-high coordinator; repository `/Users/dowwie/MyProjects/validator`.
Save acknowledgment to `docs/dispatches/validator-t028/001-coordinator-to-owner.response.md`.

## Activation and value

This authorizes T028 only, with an explicit activation condition: start its sole
developer only after the complete T026 candidate is frozen and its independent
read-only review is active. Until then, continue T026 output2 without interruption.
T028 has no implementation dependency in the accepted plan. Its private source
freeze cannot change the T026 source/test candidate. Using that otherwise idle
developer window advances a required acceptance input without another code writer.

Maintain the four active roles: owner, coordinator, one developer and one verifier.
If T026 review needs repair, finish the current atomic source-copy/checkpoint and
save T028's state before switching the sole writer; never run two developers.
T028 independent review waits until the verifier role is free. No independent
T028 acceptance is implied by copying files. T027, T029 and later remain
undispatched. This supersedes the earlier blanket later-task pause only for this
bounded T028 work and activation condition.

## Outcome, sources and safety

Use a fresh Terra-high/fork-none developer, no delegation, to deliver the exact
T028 contract: a protected frozen source bundle and persistent source-ID mapping
for all630 cases, reconciling603 labeled and27 withheld/unscored cases before
any expected metric calculation. Read complete T028, the frozen practical
acceptance and dataset-preparation spec sections, the file-dispatch rules and
applicable skills. Use python-dev for any Python work; an existing Python3.14
stdlib UUIDv7 implementation is preferable to custom UUID code if available.

The specified read-only source record is
`/Users/dowwie/MyProjects/chord/docs/experiments/typesafe-restored-score630-evaluation.md`.
Ground the inventory from it and its saved manifests. Owner's read-only metadata
inspection identified these source roots, not yet copied or accepted:

- Restored Score:
  `/Users/dowwie/MyProjects/chord/data/taxonomy/experiments/v2-typesafe-score630-20260918-r02`.
- Native Choice:
  `/Users/dowwie/MyProjects/chord/data/taxonomy/experiments/v2-typesafe-full630-20260917`.
- Choice instruction/configuration inputs referenced by the saved Score manifest:
  `/Users/dowwie/MyProjects/chord/data/taxonomy/experiments/v2-typesafe-adjudicator-20260917-r04`.

Freeze exact saved native responses for both routes, requests/full input context,
actual prompts/questions/configurations, accepted references and review/exclusion
evidence, and the saved code/record defining the native scalar-to-label mapping.
Use the existing saved `sources/` copies where available; never use moving source
code implicitly as the oracle. Record every chosen source path and exact digest.
The saved Score manifest records boundaries0.5/1.5 and Python3.14.6; the actual
mapping/tie rule must be established from the frozen source, not inferred from
argmax or a broader application policy.

Copy only evidence needed by T028/T029/T030, not the entire Chord repository or
unrelated experiment tree. Do not execute any copied operational script or
instruction, perform inference, change Chord, access credentials, or publish raw
payloads. Native outputs must remain recoverable even when a historical strict
parsed projection failed. Do not drop cases or relabel references to fit a count.

## Protected artifacts and checks

Use `/Users/dowwie/.local/share/validator/acceptance/chord630/` as the private
bundle. Inspect any existing destination first and reconcile it instead of
overwriting. Private directories must be owner-only and files owner-readable/
writable only. Retain exact bytes; do not normalize source JSON during the freeze.

Create `source-manifest.json` and `source-id-map.json` plus the exact copied source
files. The manifest records source and frozen paths, byte digests, populations,
review limitations and reference dispositions; never use a circular self-hash.
Preserve existing episode UUIDs when valid; otherwise assign UUIDv7 once to each
of the630 unique source IDs and persist the mapping. Resumption reuses this map.
Record all27 exclusions with their original reasons, and explicit cohort membership
needed for the later representative/challenge selections. Do not calculate
classifier metrics or choose a targeted subset based on desired improvement here.

Independently enumerate source IDs/dispositions and both recoverable native routes;
verify exact source-to-copy hashes and protected permissions. Record counts and
non-sensitive hash evidence publicly, with detailed/private inventories retained
inside the protected bundle. A missing native outcome or unreconciled reference
count is a named gap that blocks the complete freeze; preserve evidence and return
to owner rather than silently salvaging rows. No SQLite/state engine is needed.

Developer write scope is the protected bundle, its full file-based handoff and
only indispensable deterministic freeze tooling if a reproducible artifact is
required; coordinate any new public script ownership/indexing before writing it.
Prefer ordinary file-copy/hash/stdlib tooling over a new subsystem. Do not edit
Validator code, schemas, tests, fixtures or T026's frozen candidate. Coordinator
owns the acceptance record/index/Fizzy, and must serialize shared documentation
updates outside the frozen T026 files.

## Handoff and subsequent review

Create or update one General-board card for this source-freeze outcome, separate
from Card188; search before creating and use canonical links for dependencies.
The developer saves complete paths/hashes/counts/permissions, identity-generation
method, commands/exits, source-unchanged proof and limitations in its response.
Index the source manifest/map as protected authoritative acceptance inputs only
after independent review and owner acceptance; before that mark their candidate
status honestly. Do not expose payloads or convert the artifact index into task
state.

When the verifier role is available, use an independent Sol-high reviewer (fresh
fork-none preferred) to perform T028's bounded inventory/hash/permission/native-
recovery verification. It writes findings only, no preparation or scoring code.
One criterion-bound repair/recheck is permitted; a material interpretation gap
returns to owner. No Rust rebuild is needed for a source-only freeze. Preserve
commands and private proof, verify index links/plan/diff for public artifacts, and
return the complete handoff and verdict for owner acceptance. Do not start T029
or T030 automatically. Preserve the existing caffeinate hold and current T026
priority through its owner decision.
