# T028 conditional source-freeze acknowledgment

I read owner prompt001 in full and accept its exact activation condition. T026
output2 continues without interruption. I will not dispatch T028 until the
complete T026 candidate is frozen and its independent read-only verifier is
active. At that point the otherwise idle sole-developer slot may start one fresh
Terra-high/fork-none T028 source-freeze assignment; there will never be two
developers.

T026 keeps priority. If its review needs a repair, the T028 writer will finish the
current atomic copy/hash/checkpoint, save state and yield the writer role before a
T026 correction begins. T028's independent Sol-high review waits until the
verifier role is free. T027, T029 and later work remain undispatched.

The T028 developer will freeze only the necessary saved Chord630 acceptance
sources into `/Users/dowwie/.local/share/validator/acceptance/chord630/`, preserve
exact bytes, reconcile any existing destination without overwrite, apply owner-
only permissions and persist `source-manifest.json` plus `source-id-map.json`.
It will enumerate all 630 unique IDs, 603 labeled and 27 withheld/unscored with
original reasons, and verify both recoverable native routes. It will not execute
copied instructions/scripts, perform inference, change Chord, access credentials,
calculate metrics or select cases based on desired results.

Native requests/responses, prompts/configurations, accepted references, review/
exclusion evidence and the saved scalar-to-label mapping source will be copied and
hashed from the specified read-only roots. Existing UUIDs are preserved; otherwise
UUIDv7 identifiers are assigned once and persisted. Any missing native outcome or
unreconciled population blocks completion rather than triggering silent salvage.

I created a separate General-board card in Waiting, linked to Card188 with the
resume trigger. Protected manifests/maps remain candidate artifacts and will not
be indexed as authoritative acceptance inputs until independent T028 review and
owner acceptance. Public records will contain only non-sensitive paths, counts and
hash evidence. The existing caffeinate PID84732 hold remains active.
