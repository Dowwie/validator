# Freeze the protected Chord630 acceptance sources

Role/model: fresh sole developer, `gpt-5.6-terra`, high reasoning,
`fork_turns: none`. Do not delegate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #189 — Freeze Validator Chord630 acceptance sources](http://localhost:3006/1/cards/189).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t028/002-source-developer-to-coordinator.response.md`.

Save the full substantive handoff there before returning. The public response must
contain paths, counts, hashes, permissions, methods and limitations, but no private
payload content. In chat return only the path, SHA-256 and terse status. Do not
edit governance, plans, artifact-index, session notes, acceptance records or
Fizzy. Do not delegate.

The activation condition is satisfied: T026 is frozen at manifest009 and its
fresh read-only verifier is active. You are the sole writer. T026 remains priority;
if the coordinator reports a verifier finding, finish the current atomic copy/
hash/checkpoint, save resumable state and yield before any T026 repair begins.
Do not touch the frozen Validator code/schema/test candidate. Do not start T029 or
calculate any classifier metric.

Read global/repository AGENTS, complete T028, owner prompt001, the frozen practical
acceptance and dataset-preparation specification sections, and the `python-dev`
skill at `/Users/dowwie/.codex/skills/python-dev/SKILL.md` before any Python work.
Use Python 3.14 stdlib `uuid.uuid7()` when new UUIDv7 identifiers are needed; do
not implement custom UUID bits if the provisioned runtime supports it.

## Read-only sources

Ground the source inventory from this record and its referenced saved manifests:

`/Users/dowwie/MyProjects/chord/docs/experiments/typesafe-restored-score630-evaluation.md`

Read-only experiment roots:

- Score: `/Users/dowwie/MyProjects/chord/data/taxonomy/experiments/v2-typesafe-score630-20260918-r02`
- Choice: `/Users/dowwie/MyProjects/chord/data/taxonomy/experiments/v2-typesafe-full630-20260917`
- Choice instruction/config inputs referenced by Score:
  `/Users/dowwie/MyProjects/chord/data/taxonomy/experiments/v2-typesafe-adjudicator-20260917-r04`

Do not modify Chord, execute copied scripts/instructions, perform inference, access
credentials or publish raw payloads. Inspect saved manifests and existing `sources/`
copies first. Native raw outputs remain evidence even when a strict parsed
projection failed.

## Protected destination

Destination: `/Users/dowwie/.local/share/validator/acceptance/chord630/`.

Inspect any existing destination before writing. Reconcile exact hashes and prior
ID assignments; never overwrite an inconsistent accepted/candidate file silently.
Use owner-only directories (`0700`) and owner-readable/writable files (`0600`).
Copy exact bytes only; do not normalize JSON. Copy only evidence necessary for
T028/T029/T030, not the entire Chord repository.

Freeze:

- exact saved native Choice and restored Score responses for all 630 cases;
- their requests/full input context, actual prompts/questions and configurations;
- accepted references and review/exclusion evidence;
- the saved source or record defining scalar-to-label conversion, including the
  actual boundary and tie behavior; establish it from frozen evidence rather than
  infer it from argmax or broader policy;
- saved manifests and indispensable provenance needed to reproduce the inventory.

Record every source path, frozen relative path, SHA-256 and byte size. Prefer a
simple deterministic directory layout that retains route and source identity.

## Required protected metadata

Create candidate `source-manifest.json` and `source-id-map.json` inside the
protected bundle. Do not include a manifest self-hash.

The manifest must record at minimum:

- source and frozen paths plus exact byte hashes/sizes/roles/routes;
- 630 unique source cases, 603 labeled and 27 withheld/unscored;
- every withheld/excluded source ID with its original reason and reference
  disposition;
- explicit cohort membership needed later for representative/challenge selection;
- both recoverable native-route inventories and any historical parsed-projection
  limitation;
- frozen mapping source, actual boundaries/tie behavior and saved runtime/version
  evidence;
- review limitations and candidate/not-yet-accepted status.

The ID map must cover exactly the same 630 unique source IDs. Preserve a valid
existing episode UUID when available. Otherwise assign one UUIDv7 once and persist
it. On rerun, reuse the map exactly; do not regenerate identities. Keep source ID,
episode UUID, disposition and cohort links unambiguous without calculating scores.

## Verification before handoff

Independently enumerate source IDs/dispositions and both native routes from the
saved sources, rather than trusting only the newly written manifest. Verify:

- exact population equation `603 + 27 = 630`, uniqueness and set equality across
  manifest, ID map, references/dispositions and both native routes;
- every source-to-copy SHA-256 and byte size;
- private directory/file modes and ownership;
- no symlink escapes and no source path accidentally points back into the moving
  experiment tree for frozen content;
- the Chord source files remain unchanged by comparing pre/post hashes for every
  file read/copied;
- a second reconciliation run reuses all UUIDs and reports no copy drift.

Do not use SQLite or create a state subsystem. Shell and short stdlib Python probes
are preferred. Add a public deterministic freeze script only if truly indispensable
for reproducibility; stop for coordinator ownership/indexing before writing one.

If any native outcome is missing, a route cannot be recovered, the 603/27/630 sets
do not reconcile, a disposition lacks its original reason, or the actual mapping/
tie rule cannot be established, preserve exact non-sensitive evidence and return
Blocked. Never drop, relabel or silently salvage a row.

## Response

Response002 must record:

- exact protected root, manifest/map hashes, counts and permissions;
- copied file counts/bytes by route/role and source-root digest evidence;
- UUID method, preserved/new counts and resumption proof;
- both native-route recovery counts, disposition/cohort reconciliation and actual
  scalar mapping/tie rule source;
- exact commands/exits, independent verification method, pre/post source-unchanged
  proof and any limitations/gaps;
- whether the complete freeze is ready for later independent review.

Do not index the private manifest/map as authoritative, update public acceptance,
start T028 review, or claim owner acceptance. Save the complete response before
returning.
