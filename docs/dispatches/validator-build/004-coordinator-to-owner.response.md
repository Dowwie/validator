# Future application-boundary routing recorded

The future checkpoint decision is now recorded in the build charter, execution
contract, session notes, artifact index, and Card 183. It does not alter or dispatch
the active T002 focused recheck.

After T002 owner acceptance, the retained sole developer may receive one exact
ordered T004 -> T005 -> T006 checked-admission assignment. After owner acceptance
of that combined candidate, the next exact sequence is T007 -> T008 -> T009 ->
T010 -> T011 -> T012 -> T013 -> T014. Both retain every task contract, dependency,
named test, expected result, local prerequisite check, per-task evidence, frozen
candidate review, one-repair allowance, and owner acceptance.

The T007-T014 boundary must deliver real single-label check/evaluate through the
small application API and actual CLI, including schemas, reports, exact snapshots,
and atomic non-replacing publication. T008/T009/T013 integration evidence waits
for that real API; private internals, source inclusion, test facades, placeholders,
or fake filtered tests are prohibited. Clean format, Clippy with warnings denied,
all locked tests, and locked release build are mandatory at T014, ending the
interim dead-code allowance.

Current active worker: `/root/coordinator/verifier`, performing only the frozen
T002 repair recheck under
`docs/dispatches/validator-t002/008-coordinator-to-verifier.prompt.md`.
The developer is idle. No T004 or later implementation has been dispatched.
