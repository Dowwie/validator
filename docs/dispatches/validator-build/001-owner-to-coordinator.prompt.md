# Build Validator: coordinator dispatch 001

Role/model: coordinator, `gpt-5.6-sol`, reasoning `high`, context inheritance `none`.
Repository: `/Users/dowwie/MyProjects/validator`.
Owner: `/root`, Astra high. Required final response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-build/001-coordinator-to-owner.response.md`.
Fizzy: [Card 183 — Build Validator’s verified single-label backbone](http://localhost:3006/1/cards/183).

## Outcome and authority

The user authorized the owner to use manage-dev-team to build the complete Validator
against its specifications and plan, actively checking the team and preventing
unnecessary work/verifier drift. Your current bounded outcome is T001–T017: a real,
verified single-label steel thread. Do not start T018 or later before owner acceptance
and another numbered dispatch. Returning this handoff is not full project completion.

Read these authoritative sources before assigning work:
- `/Users/dowwie/.agents/skills/manage-dev-team/SKILL.md` in full.
- Repository `AGENTS.md`, `docs/README.md`, `docs/artifact-index.md`.
- `docs/dev-team/validator-build/charter.md` (specific owner execution decisions).
- `docs/specs/validator-v1.md` and `docs/specs/validator-data-model.md` in full.
- `docs/plans/build-validator.md`, `docs/plans/validator/execution-contract.md`.
- Relevant exact tasks T001–T017, source coverage and graph. Use these existing
  artifacts; do not regenerate the plan, research product scope, or create another
  state engine. Historical planning-only notes are superseded by the user build
  instruction and charter.

## Team and roles

Provision one `gpt-5.6-terra` high developer and one `gpt-5.6-sol` high verifier,
using fresh context with full file-based role dispatches. This hierarchical
provisioning is owner-authorized; preserve four total roles. Reuse them. No worker
spawns agents. You manage both and report to the owner. You MUST NOT write
implementation, tests, schemas, preparation scripts, or verification logic.
Developer is sole implementation writer. Verifier is read-only except saved
findings/evidence, and cannot alter acceptance fixtures. Where task metadata names
an audit author, implementation of tests/fixtures still belongs to the developer;
independent assessment belongs to the verifier. Do not substitute models/effort.

All assignments and changed instructions must first be written to uniquely numbered
prompt files under `docs/dispatches/validator-<task>/`. Include complete scope,
source paths, artifacts/interfaces, acceptance criteria, exact checks, current
baseline and prerequisite evidence, requested model/effort/context inheritance,
and mandatory saved response path. Runtime messages point to absolute files.
Read the full saved response before accepting a handoff. Index prompt/response
artifacts and link them in Fizzy. Do not demand duplicate prose already present in
accepted task/specification artifacts; reference exact context in the prompt.

## Order, evidence, bounded corrections

Confirm baseline/skills/tools and dispatch the first coherent dependency-ready task
prompt to the developer promptly. Suggested integration checkpoints are:
1. Strict checked admission/source boundaries (T001–T007, T010/T011 where ready).
2. Pure single-label scoring and check/evaluate publication (T008/T009/T012–T014).
3. Replay/inspect/default compare and independent T017 steel-thread gate.

These are checkpoints, not permission to omit or weaken individual task criteria.
Use task-sized or explicitly enumerated coherent developer assignments. Keep one
writer; do not block implementation indefinitely on process artifacts. Compile
at the first coherent path, local regression tests with each behavior, and all four
pinned checks at T017. Disclose temporary incomplete-module warnings during early
wiring; never suppress them or claim the complete gate while they remain.

Freeze exact candidate source hashes and scoped changes before verification; there
is no initial Git commit and all existing material is untracked. Preserve unrelated
material and .zvec-grep. No blanket staging, clearing build caches, or forced Git
baseline. A hash-bound snapshot with writes paused is an acceptable candidate.

Verifier checks current criteria/material regressions and highest-consequence
failures. Findings need requirement, source location, reproduction, consequence,
and smallest correction. No optional redesign, broad audit, endless test invention,
process-attestation system, or verifier-of-verifier loop. Reuse unchanged evidence;
extra checks need a concrete unresolved risk. One repair-and-recheck cycle is within
your authority; an unresolved repeat or disputed scope returns to the owner with
cause and a recommendation. Do not restart the allowance by renaming work.

Owner controls acceptance/integration. You may organize and advance dependency-ready
implementation tasks inside this tranche using accepted prerequisite evidence;
notify the owner of each verified checkpoint and request acceptance before treating
it as accepted. Continue unrelated ready work inside the tranche while an owner
decision is pending, without editing a frozen review candidate. Never advance past
T017 without the owner's next dispatch.

## Supervision and tracking

Own Fizzy card 183 operational updates, verify mutations/rendered descriptions and
first incomplete step, and keep material source/evidence links current. Read the
Fizzy skill and confirm expected Codex identity before mutation. Do not close card
183 until owner acceptance. Never create new implementation scope from findings.

Send the owner a concise check-in at each coherent milestone, before/after verifier
review, every roughly 15 minutes without a handoff, and immediately for drift,
resource surprises or material ambiguity. Include task IDs, concrete source/test
progress, active worker/command handles, remaining uncertainty and smallest next
step. The owner will periodically ask for status and may narrow verification.
These status exchanges do not change scope. Any changed assignment is file-based.

Bounds: no new provider inference or external publishing; no Chord modification
or operational commands. Do not freeze private acceptance inputs in this tranche.
Normal local Rust dependency/build/test work is authorized. Initial free disk is
approximately 17 GiB; ask owner before unusual multi-gigabyte downloads/cleanup.
Use Rust best-practices for implementation. Only use other skills when materially
needed. Do not add dependencies/abstractions to prepare hypothetical extensions.

## Return condition

Return when T017 is ready for owner acceptance or when a concrete escalation blocks
the tranche after allowed repair. Save full substantive handoff at the required
response path before messaging it: delivered behavior, task/criterion evidence,
changed files and frozen hashes, exact commands/exits/results, verifier response
path/verdict, unproven limits, observed elapsed/resources (unknowns named), active
process/worker handles and next owner decision. Keep live operational state in Fizzy.
