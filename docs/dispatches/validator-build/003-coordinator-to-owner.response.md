# Admission/type-order correction applied

The coordinator interrupted the active T003 developer before implementation bytes
were written. `Cargo.toml`, `Cargo.lock`, and `src/model.rs` still matched accepted
T001, and `src/model/common.rs` plus the superseded response did not exist.

The complete superseding developer assignment is:

`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t003/002-coordinator-to-developer.prompt.md`

Active worker: `/root/coordinator/developer`, the retained Terra-high sole writer.
It now owns corrected T003 changes to Cargo/lock for the minimal established UUID
crate, `src/model/common.rs`, and `src/model.rs`. The implemented T003 behavior is
bounded to UUID-backed identity types, SourceId/digest checks, closed TaskDefinition,
exact LabelVocabulary/LabelIndex ownership, `Episode<Target>`, and
`Outcome<Target>`, with the three original named tests.

`Prediction<Output>` is explicitly reassigned to T004 with its real typed
`ObservationSet`; T003 must not create an empty/raw observation placeholder or
implement T004 behavior. The superseded T003 prompt is preserved as Reference,
and the correction plus new prompt are indexed and linked from Card 183.

The correction also establishes the later T002 routing rule: its actual candidate
may include the necessary `src/lib.rs` parent declaration and Cargo/lock changes
for serde_json features required by strict decoding and opaque-number preservation.
No T002 code has been dispatched or written yet.

No product behavior, T001 acceptance, repair allowance, or T001–T017 boundary
changed. No verifier or external process is active; corrected T003 implementation
is now running under the original supervision cadence.
