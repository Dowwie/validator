# Accept scoring/application checkpoint and complete the backbone architecture gate

Role/model: existing Sol-high coordinator, retained context.
Repository: `/Users/dowwie/MyProjects/validator`.
Required complete acknowledgment:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t015-t017/001-coordinator-to-owner.response.md`.

## Acceptance and fixed starting point

The owner read the complete developer repair035, independent Ready verdict037,
and coordinator handoff038 from `docs/dispatches/validator-t007-t014/`. Accept the
bounded T007-T014 checkpoint at repaired manifest036 SHA-256
`9e29e12afba887851725cbabacee9ee1ec57c49299688601a3b49c0ad3f6df68`
and verifier verdict037 SHA-256
`b7185fd335f3fc472e2cd7bb6ddc693e6a4ecfda3e7082e276442aae81e1240e`.
Both demonstrated defects are repaired and independently rechecked. Formatting,
39 library + 3 CLI + 8 conformance tests, release build, diff and plan checks pass.

Acceptance carries the explicit owner028 staging decision: unmodified warning-
denied Clippy currently exits 101 for the exact 26 recorded private dead-code
diagnostics. This is not a warning-free checkpoint or a full-product completion
claim. T027/T035 must be fully clean. Preserve all existing behavior and evidence.

## Authorized sequence and decision boundary

Proceed through T015 -> T016 -> T017, with one atomic implementation handoff per
task and one combined independent review at T017. Read and use each full task JSON,
its referenced specification sections, the data model, execution contract, and
the fixed steel-thread decision in `docs/plans/build-validator.md`.

T015 implements verified replay and selected-episode inspection. T016 implements
same-population single-label comparison. T017 supplies the exact fixed end-to-end
fixture, independent rational/analytical expectations, process scenario, and saved
architecture-gate evidence. The sole developer writes permanent code, tests,
fixtures and evidence-producing scripts; the verifier reads/runs bounded checks
and writes its findings only. The task JSON's verifier implementation label does
not override this established role boundary.

After a complete local T015 handoff, dispatch T016 without a separate owner or
verifier gate; do the same for T017 after T016. Then freeze exact source/schema/
fixture/governing hashes, request the combined independent verdict, and return the
complete saved verifier/coordinator responses to the owner. Do not start T018
until owner acceptance of the architecture gate. The existing full T035 build
authorization remains in force after that decision.

## Required seams and scope limits

- Replay must verify the stored exact snapshots, deterministic one-to-one evidence
  bindings and digests, contained stored paths including symlinks, source rewrites,
  and recomputed results through the real admission/evaluation path. It must never
  dereference original evidence paths or trust unvalidated stored results.
- Inspection is the explicit payload-disclosure operation. Preserve opaque input
  fidelity, including the existing large-integer, out-of-range opaque-number and
  literal serde-number-key regressions. The retained domain input representation
  may be minimally adapted to the already-established RawValue boundary when its
  actual inspection consumer is integrated; do not round-trip opaque input through
  a lossy numeric representation. Routine reports/errors still omit raw payloads.
- Comparison uses verified runs and concrete typed metric pairs, statuses,
  populations, candidate-minus-baseline deltas, outcome transitions and explicit
  source/configuration differences. It emits no winner/significance claim. No
  intersection, multi-label or policy-expansion work belongs to this sequence.
- T016 necessarily reuses the safe publisher to publish `comparison.json` and its
  exact-file receipt. Minimal `src/artifacts.rs` changes to share the existing
  no-replace/private-temporary/cleanup implementation are expressly in scope;
  preserve the exact evaluation layout and all T012 safety evidence. Do not create
  a second publication implementation or a general artifact framework. Correct
  T016's task/physical artifact ownership mapping for this necessary seam.
- Ordinary library exports and shared model/serialization wiring required for the
  real inspect/compare application API are authorized. Expose only the small API;
  never add test facades, source inclusion, fake consumers or a broad public SDK.
- T017 uses the exact fixed four-row oracle in the master plan. In particular,
  baseline macro-F1 is 1/2 and Brier is 0.30; observations on abstention remain
  observations, all four probability rows count, and only three raw answered rows
  enter baseline confidence bins. Expected values must not come from production
  scoring. Demonstrate relocation with original evidence unavailable, inspection,
  comparison, output schemas, receipts, privacy and the listed failure cases.

The architecture hypothesis and decision remain those stated before build in the
master plan: exact-byte checked admission, separate observations/scoring, safe
publication and verified recomputation must support relocated inspection and
comparison without original sources. A pass permits the wider task semantics;
a broken seam is repaired here, without fallback routes or changed tolerances.

## Team operation, evidence and limits

Keep one Terra-high developer and one independent Sol-high verifier, with the
existing Sol-high coordinator and Astra-high owner. Reuse available workers where
useful; a fresh Terra-high/fork-none replacement is authorized for a new atomic
task or another incomplete retained-context handoff. Preserve valid work, stop the
old writer first, and record any interruption honestly. No worker delegates.
Every dispatch/follow-up uses full numbered prompt and response files, including
global and repository instructions and required skills for a fresh context.

Each local handoff must demonstrate actual named assertions and behavior, not just
passing filter names. Run affected checks during implementation; at T017 run the
exact planned full format, warning-denied Clippy, all-tests and release commands.
Only the previously enumerated staged dead-code diagnostics may remain, with their
real exit status and exact residual symbols disclosed. Required consumer wiring
may reduce that inventory; no other warning or failed behavior/build check is
covered. The warning-free T027/T035 floor is unchanged.

Use one combined review and one coordinator-managed repair/recheck allowance for
this new T015-T017 checkpoint. Findings must identify a current violated criterion,
location, reproduction, consequence and smallest correction. No speculative audit,
optional coverage, unrelated cleanup or verification framework. Reuse unchanged
valid evidence; coordinator freeze work is hash/criterion reconciliation, not a
routine third run of the developer and verifier's complete checks. Return repeated
failure or a real scope conflict to the owner with the changed approach needed.

Record this acceptance and sequence in the charter/execution contract and session
notes; keep active ownership/dependencies/next actions in Card183, which remains
Working On until the backbone gate is accepted. Index and link all artifacts and
verify the plan/index links and diff checks after governance changes. Preserve the
unborn/untracked workspace, private source boundaries, and caffeinate PID84732.
Continue concise milestone/blocker check-ins; the owner remains actively supervising.
