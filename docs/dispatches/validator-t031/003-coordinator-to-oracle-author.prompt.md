# Refreeze the literal-minimum two-episode T031 bundle

Resume the retained independent oracle author as sole writer. Required repair
response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t031/003-oracle-author-to-coordinator.response.md`.

Read verifier verdict002 SHA-256
`80dc03b2ea78b223ee26be861358b5abc88f7848c857f0803636f98b3a894b92`.
Completion002/003 requires the smallest case the specified behaviors permit. Use
exactly two frozen episodes:

1. expected `{A,B}`, baseline `{A,C}`, candidate `{A,B}`: overlapping known
   failure and recovery;
2. expected `{}`, baseline `{}`, candidate whole abstention with complete
   marginals: answered empty set and regression.

Both runs remain compatible/full. Targeted candidate selects only episode 1, a
strict nonempty subset; intersection excludes episode 2. Keep complete marginals
needed for exact probability evidence.

Refreeze all six input/config/prediction bytes first with new hashes. Only then
independently rederive every expected raw/final hard metric, per-label counts,
exact rational Brier, analytic logs, statuses, selected/answered IDs, known
failure, recovered/regressed/changed IDs and all per-label transitions. Correct
label-state transitions from outcomes, independent of reference. Write expected,
then manifest last with seven non-self hashes.

Do not run Validator, read production/private results, add files/scripts/cases or
retain obsolete four-case values. Run only JSON/hash/schema-shape and temporary
arithmetic checks. Save exact identities/derivation in response003, then stop.
