# Independently verify the frozen multi-label practical oracle

Role/model: fresh independent verifier, `gpt-5.6-sol`, high reasoning,
`fork_turns: none`. Do not delegate. You did not author these fixtures.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #193 — Freeze Validator multi-label practical oracle](http://localhost:3006/1/cards/193).

Required response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t031/002-verifier-to-coordinator.response.md`.

Save Ready/Revise/Blocked before returning only path/SHA/verdict. Do not edit
fixtures/repository/Fizzy or run Validator. Do not delegate.

Read T031, its normative metric/data-model/practical sections and complete author
response001. Reconcile six frozen input hashes, expected hash and manifest entries;
confirm manifest excludes itself and no generated run output exists.

Independently derive every expected entry from frozen inputs: raw/final N/G/U/D/E,
per-label TP/FP/FN/TN and metric statuses, exact-set/micro/macro/Hamming values,
selected/answered IDs, per-episode evidence, exact rational Brier `1/25`, analytic
`-ln(4/5)` logs, full and targeted/intersection populations, recovered/regressed/
changed IDs and all per-label transitions. Confirm the four-case bundle is the
smallest case satisfying overlapping/empty sets, complete-marginal abstention,
known failure, two compatible runs and partial overlap.

Use only temporary arithmetic/read-only schema probes. No product scoring, extra
fixtures, maintained checker or broader audit. Return Ready when all T031 criteria
match; Revise only with cited exact required defect and smallest correction; stop.
