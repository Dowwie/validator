# Freeze the minimal independent multi-label practical bundle

Role/model: fresh independent oracle author, `gpt-5.6-terra`, high reasoning,
`fork_turns: none`. Do not delegate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #193 — Freeze Validator multi-label practical oracle](http://localhost:3006/1/cards/193).

Required response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t031/001-oracle-author-to-coordinator.response.md`.

Save the full handoff before returning only path/SHA/status. You are sole writer
while T029 has a read-only verifier. Do not delegate.

Read global/repository AGENTS, complete T031, and only its normative multi-label
metric/data-model/practical-case sections and published input schemas. Do not read
production scoring, Validator outputs, T029/T030 private artifacts/results, earlier
production-derived expected fixtures or internal numerical handoffs.

Own only the eight listed `tests/fixtures/acceptance-multi/` files and response001.
No product/test-code/script/framework/docs/governance edits. Keep the public case
as small as possible while covering overlapping/empty expected and predicted sets,
whole abstention with complete marginals, one known failure, two compatible runs
and partial selection overlap.

Write/freeze the seven input/config/prediction fixture bytes first and record their
hashes. Only afterward derive `expected.json` independently from those frozen
inputs: exact raw/final counts, per-label/aggregate metrics, rational Brier,
analytic log expressions, statuses, selected/answered IDs, known failure,
recovered/regressed/changed IDs and per-label transitions for full/default/
intersection/targeted behavior required by T031. Use exact rational/integer
operands; do not run Validator to obtain or confirm expected values.

Write `manifest.json` last with input/oracle hashes and derivation, excluding its
own hash. Generated Validator run output is not a fixture. Use only temporary
one-off arithmetic probes, no maintained oracle program. Validate JSON/schema
shape where that does not invoke scoring. Record the freeze-before-calculation
sequence, hashes, independent arithmetic, commands/exits and limitations in the
response. Stop for a fresh reviewer before any Validator evaluation.
