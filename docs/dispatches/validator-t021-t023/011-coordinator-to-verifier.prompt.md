# Add zero-exclusion explicit-intersection reproduction to review009

Role/model/context remain the active independent verifier from prompt009,
`gpt-5.6-sol`, high reasoning, `fork_turns: none`. Do not delegate. Keep frozen
manifest008 and every source/schema/test file read-only.

Continue to save the complete combined verdict at the existing required path:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t021-t023/009-verifier-to-coordinator.response.md`.

Read owner prompt010 and coordinator acknowledgment010 in full. Add only this
current T023 concern to the existing bounded review:

1. Run explicit `--intersection` on compatible runs whose selected populations
   are equal and nonempty, through both the single-label and multi-label branches.
2. Run the same explicit option on compatible equal empty selected populations
   through both task branches.
3. Check application/CLI behavior, the requested/emitted scope, population lists
   and counts, and validate any emitted `comparison.json` against
   `schemas/v2/comparison.schema.json`.

The ratified contract permits explicit intersection and does not require different
selections or an exclusion. Inspect the interaction between
`comparison::intersection_scope` and the schema's `intersection_population`
definition. Determine whether a legal zero-exclusion intersection is rejected or
an invalid document is emitted. Reuse existing fixtures or temporary external
inputs; do not edit the candidate or create repository test machinery.

Include exact commands/input shape, both task results, schema result and conclusion
in the existing response009 verdict. If reproduced, return **Revise** and identify
the smallest correction that admits zero-exclusion intersection while preserving
strict identical-scope population constraints and task-family discrimination. Do
not propose artificial exclusions or silently convert the requested scope. If not
reproduced, explain the concrete production path that makes it legal. Stop after
this and the original prompt009 obligations support one verdict; no broader schema
audit, new cycle or T024 work.
