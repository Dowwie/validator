# Apply owner evidence-placement clarification to recheck014

Role/model/context remain the same independent verifier completing prompt014.
Keep repaired manifest013 frozen and save the final verdict at the existing path:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t021-t023/014-verifier-to-coordinator.response.md`.

Read owner prompt015 and coordinator acknowledgment015 in full. Resolve the noted
evidence-placement question as follows:

- assess the four repaired cases across the existing conformance regression, CLI
  regression and your independent actual-binary probe collectively;
- record precisely that conformance asserts recomputed typed values/statuses;
- record precisely that the CLI test asserts explicit routing/scope, population,
  zero exclusions, schema validity and receipt digest;
- record the typed values/statuses and all other facts you independently checked
  through the real binary for all four cases;
- do not claim the CLI test contains typed-result assertions it lacks.

The original T023 evidence ownership assigns recomputation to conformance and flag
routing/receipts/exclusions to CLI. It does not require identical assertions in
both automated bodies when the combined evidence proves the behavior. If the
existing evidence proves the repaired requirement and no other current-contract
defect exists, no duplicate-assertion patch is required. Report any concrete
unproven behavior normally; do not suppress a material finding.

No test, oracle, production code, schema, tolerance or mandatory gate is waived or
weakened. Do not start another audit or review cycle. Finish and save the one
focused Ready/Revise/Blocked verdict once current Clippy/diff and this evidence
mapping are complete.
