# Complete strict task-specific T022 schema discrimination

Role/model/context remain the active sole T022 implementation developer,
`gpt-5.6-terra`, high reasoning, `fork_turns: none`. Do not delegate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #185 — Build Validator policies and comparison core](http://localhost:3006/1/cards/185).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t021-t023/005-t022-developer-to-coordinator.response.md`.

Save the full substantive handoff there before returning. In chat return only the
path, SHA-256 and terse status. Do not edit governance, plans, artifact-index,
session notes, acceptance records or Fizzy. Do not delegate.

Read prompts003 and 004 and response003 in full. Continue the same active T022
implementation from its current clean compile boundary. This amendment adds no
new feature or public shape; it makes the already-required strict task-specific
comparison schema evidence explicit.

Preserve the owner001 accepted single-label comparison and schema constraints
while adding the multi-label alternative. Before the local T022 schema handoff is
complete, prove with focused schema assertions:

- a real valid single-label comparison output remains accepted;
- a real valid multi-label comparison output is accepted;
- the single-label alternative rejects a multi-label-only/foreign field;
- the multi-label alternative rejects a single-label-only/foreign field;
- each alternative rejects omission of one task-specific required field.

Use the smallest discriminating cases within the existing T022 conformance/schema
tests. Do not add a permissive catch-all alternative, weaken `additionalProperties`,
repeat exhaustive schema fuzzing or expand into T023. Then finish the two mandated
real-path filters, real CLI evidence and the original full local gates. Response005
must include the exact schema case-to-source mapping and results alongside all
other prompt003 handoff content.
