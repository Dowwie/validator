# Accept multi-label core and deliver policies and comparison

Role/model: existing coordinator, `gpt-5.6-sol`, high reasoning. Repository:
`/Users/dowwie/MyProjects/validator`. Follow manage-dev-team and repository
file-based dispatch rules. No implementation, test or verification-code authorship.
Save the full acknowledgment to
`docs/dispatches/validator-t021-t023/001-coordinator-to-owner.response.md`.

## Owner acceptance

The owner read the complete original verdict023, repaired Ready verdict026 and
coordinator handoff027 under `docs/dispatches/validator-t018-t020/`. Accept the
exact repaired T018-T020 candidate:

- manifest025: `e26438bd371f261e5d91a05d55312bfa1d329db75dc4e24e761d75d6bd646f11`;
- Ready verdict026: `7e3885c40aced13a3822d1b3d3564c292035821c0902da3970923ee68f84cae2`;
- handoff027: `a145686412b74ae7c606d1acb98a27100f182cf8a71f2ebedd13aba630fd3fd3`.

Both task kinds now have verified check/evaluate/replay/inspect paths, concrete
hard/probability semantics and strict output schemas. The unused foreign source
and corresponding report-schema findings are resolved with neighboring legal
cases preserved. All 66 tests, formatting, release and plan/index checks pass.
Unmodified Clippy still exits 101 for exactly 20 authorized staged diagnostics;
this is neither a clean gate nor full-product completion. The warning-free
T027/T035 gates and all practical/fresh-agent acceptance remain required.

Record this acceptance in the existing acceptance record/session decisions without
claiming release. Complete Card184 after recording accepted evidence. Search Fizzy
for the next relevant outcome before creating its General-board card; link its
dependency to Card184's canonical URL and keep all current operational state there.

## Authorized ordered sequence

Deliver T021 -> T022 -> T023 from the ratified task contracts. These are ordinary
incremental extensions of the already-tested design. Use one fresh Terra-high,
fork-none sole writer per atomic task and one independent Sol-high verifier after
the complete T023 integration boundary. Read each complete task and its linked
normative sections before dispatch; the specification prevails over summaries.
No T024 or later work before this checkpoint returns for owner acceptance.

Local handoffs advance automatically after complete evidence; they are not separate
owner approval gates. Keep immediate dispatches scoped and concise. The prior T020
failure pattern was dense cross-layer completion returned partially; when a task
has a dense implementation/schema/evidence boundary, allow separate local handoffs
for those outputs within its existing contract, with fresh context for a substantial
next output. Do not split by arbitrary file count or create new product scope.
One writer remains active at any time. No worker delegation or model substitution.

## T021: explicit task-specific policies

Implement both checked policy variants, admission preconditions and real final
decision application. Preserve raw outcomes/results and the original scoring and
signal populations. Required semantics include:

- Single-label `reject_below`: confidence or working-vector maximum probability,
  finite minimum in [0,1], exact equality accepted and adjacent binary64 values
  discriminated. Every supplied row must already be answered and contain the
  requested scoring signal; observations never substitute. Preserve scored-choice
  reported confidence versus maximum probability distinction and general-classifier
  recorded choice versus argmax distinction. Final rejection carries the specified
  reason. Raw-confidence bins retain later rejected rows and IDs.
- Multi-label `label_thresholds`: exact complete vocabulary map of finite [0,1]
  thresholds, complete marginals and already answered rows. Select each label on
  `p >= threshold`; all-below gives an answered empty set, not abstention. Preserve
  original sets/marginals and raw metrics; no normalization/defaults/partial maps/
  policy chaining or synthesized answers.
- Wrong task, missing required family and submitted abstention reject with the
  ratified typed diagnostic before scoring. Policy configuration and effective
  report/inspection evidence must expose what was actually applied.

Use the existing wire/model/evaluator/application boundaries. In addition to the
T021 listed artifacts, necessary policy serialization and report/inspection schema
branches are owned here: current output schemas admit only as_recorded, so they
must evolve with actual policy outputs. The existing input schemas already describe
both policies; change them only if a concrete current-contract mismatch requires
it. Preserve strict task alternatives and all previously accepted constraints.
Do not defer valid actual policy documents to T024. Update task/physical mappings
for these necessary real integration artifacts rather than treating wiring as an
out-of-scope blocker. Run both exact T021 conformance filters through the real
application/report path and record independent boundary expectations.

## T022: typed multi-label comparison

Build the concrete multi-label comparison and wire the existing compare API/CLI
through closed verified task dispatch. The app/CLI/model/report-serialization
changes needed to call it are part of this task even where listed only as inputs.
Reuse snapshot/evidence verification, publication, common identity/compatibility,
source/preparation difference reporting and checked arithmetic. Retain the accepted
single-label comparison behavior.

Compare typed recomputed MultiLabelEvaluation/results, not serialized report JSON.
Use explicit typed metric-pair/result fields and task-specific populations. Do not
reintroduce recursive metric discovery, string-key metric maps, a generic evaluator
registry or a universal accuracy abstraction. Emit concrete report-schema
alternatives in `schemas/v2/comparison.schema.json` and validate real outputs.

Required evidence: exact-set final correctness categories sum to common N; changed
outcomes retain IDs even when both sets remain wrong; each label has a complete
absent/present/abstained 3x3 table summing to N; whole abstention maps every label to
abstained, without power-set classes. Each side retains outcomes, correctness,
local source ID, matched/missed/extra and abstention state. Raw/final paired metrics
remain separate; answered-only results expose both answered ID/count populations
and overlap, with conditional-difference semantics. Numeric deltas require both
statuses defined; otherwise null with reason, preserving metric direction. No
winner/significance claim. Source IDs are local; compare definitions, observation
definitions and preparation differences rather than just matching IDs.

Default comparison retains all existing compatibility requirements. Run both exact
T022 filters using real public comparison artifacts and real binary multi-label
compare evidence. Extend only the existing CLI harness as needed; no fake facade.

## T023: validated intersection and recomputation

Implement the explicit --intersection API/CLI option and checked restriction on
the existing validated structures. Restrict to existing common selected IDs, retain
task/vocabulary/policy/source/provenance ownership and original artifact-wide
SignalAvailability, then recompute both concrete result families on those rows.
Never synthesize/redecode prediction JSON or subtract original aggregate results.

Default unequal selected populations still reject. Intersection allows selection
differences only; golden digest, task, ordered vocabulary, role and numerical
semantics must still match. Emit deterministic compared and both excluded IDs/
counts, compared-population source attribution, original run/report identities,
explicit scope and recomputed pairs. Preserve raw/final answered populations and
overlap; a targeted result is not a full-population improvement claim.

An empty intersection is valid. Applicable signal metrics retain no_data, absent
families retain not_applicable independently for each side/task, and no undefined
case emits a numeric delta. Preserve the legal policy of a source run while
restricting; never infer absence from zero restricted rows. Cover every original
empty-intersection availability combination and both task kinds in the exact two
conformance filters and CLI filter. Necessary `src/app.rs` API wiring and comparison
schema changes are owned here; synchronize the task/physical map accordingly.

## Completion, review and controls

All exact named T021-T023 filters must list and execute nonzero on their required
targets. Map criteria to the actual production path, independent expected values,
status/unit/population assertions and passing/failing cases. A test name alone is
not evidence. Preserve accepted opaque RawValue/large-number/null/marker behavior,
relocated bound-evidence replay/tamper protection, immutable output refusal and
single-label/multi-label regressions. Do not loosen fixture tolerances or schemas.

Each developer runs focused checks at its coherent path; final T023 runs the full
pinned fmt, unmodified warning-denied Clippy, locked tests, release and diff gates.
The existing exact 20 staged private diagnostics may remain only as documented;
real T021/T022/T023 consumers should remove the appropriate portion. Do not invent
future consumers, delete mandated contract validation, widen exports or fake uses.
No ordinary/new warning class is permitted. Mechanical corrections required for
mandatory gates within owned implementation scope are ordinary completion work.
Final T027/T035 remain completely warning-free; do not perform unrelated cleanup now.

Freeze exact hashes after complete local evidence and use one fresh independent
Sol-high verifier for the full T021-T023 candidate. It must inspect the scoped
code/schema changes, verify actual criteria and independent expected results, and
return one saved Ready/Revise/Blocked verdict. No future-task audit or optional
architecture/style requests. Reuse unchanged evidence; do not run a habitual third
coordinator full suite. One finding-driven repair/recheck cycle is authorized for
this new checkpoint. Repeated incomplete output or unresolved material conflict
returns to the owner with the actual cause, changed approach and bounded next result.

Keep Fizzy current with verified rendered descriptions and the first incomplete
step, index every dispatch/response/accepted artifact, maintain required mappings,
and run plan/index/diff checks. Preserve the unborn/untracked workspace and all
unrelated files; no staging, commit, reset or broad cleanup. Maintain the existing
caffeinate hold, serialize competing Cargo commands, and return the final combined
handoff/verdict to the owner before scheduling T024.
