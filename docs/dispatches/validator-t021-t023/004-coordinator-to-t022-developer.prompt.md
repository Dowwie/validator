# Correct T022 hash label and resume unchanged implementation

Role/model/context remain the active sole implementation developer,
`gpt-5.6-terra`, high reasoning, `fork_turns: none`. Do not delegate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #185 — Build Validator policies and comparison core](http://localhost:3006/1/cards/185).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t021-t023/004-t022-developer-to-coordinator.response.md`.

Save the full substantive handoff there before returning. In chat return only the
path, SHA-256 and terse status. Do not edit governance, plans, artifact-index,
session notes, acceptance records or Fizzy. Do not delegate.

This numbered correction supersedes only the ambiguous hash label in prompt003.
Read prompt003 and response003 in full, then resume its unchanged implementation,
scope, criteria, constraints and checks. No implementation file changed during
the stopped attempt.

The prompt003 starting-hash row labeled `T022 task` means the ratified task
contract at `docs/plans/validator/tasks/T022.json`. Its observed SHA-256 is the
declared value:

```text
28ecbba28f7d6c7c6a133a9018862abc0e96733b8da617b39aa8493a40ba9608
```

It does not mean prompt003. Prompt003 is the coordinator dispatch and its
expected/observed SHA-256 is:

```text
286e6ecc56ccff63e6fd2a5aa352514ef22b796dc36f1b0986a18392c83dec9b
```

The correct schema path is `schemas/v2/comparison.schema.json`; its expected and
observed SHA-256 remains:

```text
24002dce44ffe08c9900c71a2db3a5c81e7b5b274bd05fd647c46468bf2c75d6
```

All other prompt003 starting hashes were reconciled in response003. Confirm the
three identities above, then proceed immediately with the complete T022 work and
save response004. Stop only for a new source/contract mismatch or demonstrated
implementation blocker. This clarification does not change or add any criterion.
