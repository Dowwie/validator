# Resolve the focused recheck's evidence-placement question

Existing Sol-high coordinator; repository `/Users/dowwie/MyProjects/validator`.
Save acknowledgment to
`docs/dispatches/validator-t021-t023/015-coordinator-to-owner.response.md`.

The reported recheck establishes all four corrected equal-selection cases through
the real API and an independent binary probe, including typed values/statuses,
scope, population, schema and receipt. The automated CLI regression covers scope,
population, schema and receipt; the automated conformance regression covers the
recomputed typed values/statuses. The candidate is stable and no behavior defect
has been reported. Owner read prompts012/014 and the original T023 contract.

The original repair criterion is to establish those facts through the existing
conformance and CLI paths. It does not require every assertion to be duplicated
in both automated test bodies. T023 assigns metric recomputation to conformance
and flag routing/receipts/exclusions to CLI evidence. Treat the actual evidence
collectively, including the independently executed four-case binary probe.

Relay this clarification to the current verifier by numbered file addendum. It
must record precisely which facts are automated where and which binary assertions
it executed independently. Do not claim the CLI test contains assertions it lacks.
If that existing evidence proves the repaired requirement and there is no other
current-contract defect, a further duplicate-assertion patch is not required for
this verdict. If a concrete unproven behavior or reproduction remains, report it
normally; do not suppress a material finding.

No test, oracle, production code, schema, tolerance or mandatory gate is weakened
or waived. This is an evidence-placement clarification preventing an unnecessary
second correction cycle, not a new test framework or a broadened review. Preserve
the frozen candidate, finish the single saved verdict and return the combined
handoff. Index/link this decision and responses; keep operational state in Fizzy.
