# Accept the architecture gate and build the concrete multi-label core

Role/model: existing Sol-high coordinator, retained context.
Repository: `/Users/dowwie/MyProjects/validator`.
Required full acknowledgment:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t018-t020/001-coordinator-to-owner.response.md`.

## Architecture decision

The owner read the full independent Ready verdict014 and coordinator handoff015
from `docs/dispatches/validator-t015-t017/`, plus the T017 developer evidence.
Accept the architecture gate at manifest013 SHA-256
`46dac241d71c838ce1251ae9ed7914798c84009eeb937a7577f5c16da735c5c3`
and verifier verdict014 SHA-256
`12019ccc02925b73697686257587c7ea99abdf847c72cc44068056bd90e40a82`.
The independent oracle, four real CLI paths, relocated replay, evidence binding,
typed comparison and shared safe publication support the planned wider build.

The incorrectly targeted relocation command in the review prompt is a clerical
dispatch error: the verifier ran the correct CLI filter with one test and the
complete suite. Preserve that honest record; no further test or repair is needed.

This acceptance covers the single-label architecture gate, not the entire utility.
Forty unit, six CLI and eleven conformance tests, formatting and release build pass.
Unmodified Clippy still exits 101 for the explicitly staged 24 diagnostics. The
warning-free T027/T035 gates and full private/fresh-agent acceptance remain required.
Record the accepted gate in `docs/acceptance/validator-v1.md`, its index entry and
session notes without claiming release or full-product completion. Complete Card183
after recording this owner decision, then resolve/create the next General-board
card under the Fizzy contracts, linking its dependency to Card183's canonical URL.

## Next bounded sequence

Authorize T018 -> T019 -> T020 as one coherent multi-label admission/scoring/command
candidate, with an atomic local handoff for each task and one independent review
after T020. Read each complete task JSON and every referenced normative section.
Do not begin T021 or later work before the owner accepts this next checkpoint.
The full T035 build authorization remains active; later work follows acceptance.

Use a fresh Terra-high/fork-none sole developer for each substantial atomic task,
given the repeated retained-context incomplete handoffs. Preserve exact source and
response bindings between tasks and stop the prior writer before replacement. Keep
the prescribed Sol-high coordinator and independent Sol-high verifier, at most four
active roles. Workers never delegate. All instructions and full responses remain
numbered file-based artifacts with model/reasoning/context settings and required
source/skill context; no chat-only handoffs.

## Construction order without stubs or false integration evidence

T018 implements the concrete checked multi-label structures and actual shared
wire-to-domain admission, with its two owning-library tests. T019 implements actual
hard accounting and independent expected results, with owning-module tests first.
T020 adds actual marginal losses/bins and completes the real check/evaluate/inspect/
replay integration and typed report serialization for both tasks.

There is a real forward dependency in the original test placement: T019's two
conformance filters need T020's real application path. Record this explicitly in
the task/execution wording. Implement and test the local T019 invariants now, then
make the exact named conformance filters real and nonzero through the production
API at T020. Do not invent a facade, expose internals for tests, include production
source, add fake consumers, or report a zero-selected filter as passed evidence.

Complete application-facing closed dispatch when both implementations exist at
T020; private checked types and hard evaluation can be built/tested earlier without
unimplemented success paths or placeholder marginal results. Preserve every working
single-label command throughout. Current operational policy scope is as_recorded;
T021 owns actual reject_below/label_thresholds application. Never silently evaluate
an unsupported policy as as_recorded. T022 owns multi-label paired comparison, as
already stated in T020's command-test contract. No intersection or policy expansion
belongs to this sequence.

## Required structure and semantics

- Reuse the shared source/observation, UUID, exact-byte, evidence, selection/alignment,
  replay and publication backbone. Do not copy these implementations for the second
  task. Factor only actual shared responsibilities while keeping each task's
  denominators, target semantics and probability semantics concrete.
- Integrate the existing checked task, vocabulary, index/set and episode contracts
  into their actual consumers where required. Use closed task dispatch and separate
  MultiLabelEvaluation/Output/Policy/Results types. No foreign-vocabulary indexes,
  inferred task kinds, cross-kind policy/probability coercion, dynamic metric map,
  universal accuracy abstraction or generic evaluator framework.
- Preserve RawValue fidelity for opaque input/source/preparation data, including
  existing large-integer, 1e400 and literal serde-number-key regressions. No cloning
  of opaque payloads into metric rows or comparison results and no fake consumers
  merely to reduce the warning count.
- Empty label sets are answered. Abstention, missing rows and missing marginal keys
  remain distinct. Reject duplicate submitted set labels before set construction;
  preserve vocabulary order. Reject scored_choice, top-level scoring confidence,
  count/multiset/partial-label/per-label-abstention shapes and wrong task bindings.
  Retained reported-confidence observations remain legal evidence.
- Marginals are finite independent [0,1] values with exact vocabulary keys. Never
  normalize across labels or synthesize argmax, joint set probability, confidence
  or ECE. Probabilities on abstention still count over the selected population.
- Hard accounting uses N references and G answered episodes: per-label four counts
  sum to G; total label decisions sum to G*K using checked arithmetic. Exact-set,
  selective, micro/macro and Hamming results retain their specified populations,
  units, statuses and denominators. Abstentions have null matched/missed/extra sets
  with an explicit status; answered empty sets retain genuine empty sets.
- Use the independent stated marginal oracles, including [0.8,0.7] against {A}
  yielding mean Brier .265 and (-ln(.8)-ln(.3))/2, and one-label binary Brier .04
  versus two-class categorical .08. Handle actual zero-probability outcomes without
  0*ln(0); retain explicit infinity and correct no-data/absent-family precedence.
  Per-label bins reflect reference presence, not classifier correctness.

## Necessary integration ownership

T020 must add complete concrete multi-label alternatives to the existing report,
inspection and check schemas where their actual new output requires them; receipt
and error remain task-neutral unless a demonstrated mismatch requires correction.
T024 retains the comprehensive nine-schema audit. Do not leave real T020 output
outside its published serialization contract or add permissive placeholder schema
branches. Typed Rust fields and strict schema properties must agree.

Minimal shared model/accessor, library export, app/replay/CLI, test and schema wiring
needed by T018-T020 is expressly authorized. Update affected task/physical/input
artifact mappings in the same governance change. Do not create a second filesystem
path, a public SDK expansion, extra dependencies without need, or a schema framework.

## Verification and supervision

Advance T018 -> T019 -> T020 automatically on complete local evidence, without
separate owner/verifier gates. At T020 every exact task filter must exist and run:
`multi_label_checked_admission`, `cross_task_boundaries`,
`multi_label_hard_oracles`, `equal_counts_distinct_exact_sets`,
`marginal_loss_and_bins`, and `shared_commands_multi_label` on their task-specified
targets. The conformance/process cases must use the real public API/binary. Preserve
all accepted single-label/replay/comparison/steel-thread regressions.

Run full pinned formatting, unmodified warning-denied Clippy, locked tests and
release build at the combined boundary. No ordinary/new lint class is allowed.
Retain exact residual staging records for the existing private symbols. If the
new checked policy/DTO structures have unavoidable private members whose real
consumers are explicitly T021/T022, identify each symbol and actual owning task;
those may be staged only until those integrations, with T027 still fully clean.
Do not use this for speculative helpers, guessed later consumers or suppression.

Freeze exact hashes for one independent T018-T020 review. Its findings must cite
the current criterion, source location, reproduction, consequence and smallest
correction. One coordinator-managed repair/recheck cycle is authorized for this
new checkpoint; repeated incomplete work or a material unresolved conflict returns
to the owner with a changed approach. Reuse unchanged valid evidence; no routine
coordinator replay of the developer/verifier's complete suites, unrelated audit,
optional cleanup or verification framework.

Update charter/execution decisions and the applicable task/physical mappings,
verify plan/index links and diff checks, and keep actionable status in Fizzy.
Index/link every dispatch and response. Preserve the unborn/untracked workspace,
private acceptance boundaries and caffeinate PID84732. Continue periodic concrete
milestone/blocker check-ins; the owner remains responsible through the full DoD.
