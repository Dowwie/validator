# Complete T020 from a fresh, bounded context

Role: existing Sol-high coordinator. Repository:
`/Users/dowwie/MyProjects/validator`. No implementation or test authorship.
Save the full acknowledgment and resumption record to
`docs/dispatches/validator-t018-t020/009-coordinator-to-owner.response.md`.

The owner read complete responses005,007,008 and the affected new test bodies.
Response008 SHA-256 is
`88750cc6b8b541bbce36d8ce6534505dc5704817eaa10659f7b6bfa6e073e5d7`.
No candidate is accepted or ready for independent review yet.

## Reassessment and changed approach

The first T020 writer delivered a working partial scorer/application path but twice
returned before satisfying its bounded contract. The evidence establishes repeated
incomplete delivery, not an unavailable dependency or specification conflict. A
retained-context retry did not complete the remaining criteria. Retire that writer
and provision one fresh `gpt-5.6-terra`, high reasoning, `fork_turns: none` developer.
This is the sole implementation writer; do not delegate further or change models.

Authorize a focused completion of the existing T020 contract from the hashes in
response008. Preserve valid code and passing evidence. The remaining work has
material value: strict machine contracts and discriminating numerical tests are
necessary for the requested usable multi-label evaluator and its independent
review. Do not restart T020 or broaden the product or review scope.

## Exact completion scope

Read governing owner001 and correction006, task T020, the original required
criteria in developer prompts005/007, responses005/007/008, the exact relevant
specification sections, and current owned files before editing. All prior
acceptance conditions remain unchanged. In particular:

1. Finish strict check/report/inspection schema alternatives from actual concrete
   contracts. Enforce required nested fields, closed properties, null/status
   alternatives, and foreign-task rejection. Preserve accepted single-label
   constraints; a permissive rewrite is a regression even if positive examples
   pass. Use a valid document and focused invalid mutations to discriminate these
   constraints. Do not add placeholder branches or defer their validity to T024.
2. Complete `multi_label_hard_oracles` through real public evaluation and the
   published report using the existing independent T019 oracle. Its current body
   checks only one row's exact counts and matched/missed/extra sets; it does not
   yet establish the promised aggregate rational metrics, population units/scopes,
   answered-empty, abstention, all-abstained and empty-population statuses. Cover
   the original oracle and criterion map, preserving raw/final evidence. Do not
   treat a nonzero test count as proof of its advertised criterion.
3. Correct `equal_counts_distinct_exact_sets` to the actual two-row pair with
   equal per-label binary counts and distinct exact-set accuracy. Checking only
   equal label-array lengths is insufficient. Reuse T019's independent fixture
   and derivation without changing expected values to fit production results.
4. Complete the existing `marginal_loss_and_bins` cases: finite abstention oracle,
   absent-at-zero zero loss, observed-zero-probability infinity at both endpoints,
   one-label binary Brier .04 versus categorical .08, and reference-positive bins
   at 0/.1/1. Preserve exact population/unit/status checks and unchanged marginals.
5. Preserve the working real CLI check/evaluate/relocated-inspect path. Finish its
   original relevant privacy, exact payload, and output-refusal assertions; reuse
   accepted shared-artifact regression evidence instead of duplicating its entire
   failure matrix. Both task branches must still use the same publication/replay
   boundary. No multi-label comparison or threshold implementation is authorized.

Primary edits are the three schemas and existing conformance/CLI tests. Minimal
production corrections are allowed only for a reproduced violation of this same
T020 contract; report requirement, reproduction and changed paths. Do not refactor
the working evaluator, alter tolerances, widen exports, add dependencies, suppress
diagnostics or manufacture consumers.

## Evidence and return boundary

Run all six original exact nonzero filters and verify their correct list targets,
then the original full fmt, unmodified Clippy, locked tests, release build and
diff checks once on the complete candidate. Apply owner001/correction006's exact
existing-versus-new staged-warning distinction. T027/T035 remain warning-free.
Preserve all original raw-value, single-label and replay regression assertions.

Require a complete saved handoff with criterion-to-production-test mapping,
expected/actual results, commands/exits, final hashes and exact residual lint
inventory. Read it before freezing. This context replacement does not reset or
hide the repeated-incomplete history: any further incomplete return requires an
owner reassessment, not an automatic restart. The single independent combined
T018-T020 review and its one finding-driven repair/recheck allowance remain as
authorized in owner001; neither has started.

Index/link this prompt, its acknowledgment and the new developer dispatch/response.
Resume Card184 from Waiting to Working On when the fresh writer is active, verify
rendered content and first incomplete step, and maintain the existing caffeinate
hold. Return the complete combined handoff for owner acceptance before T021.
