# Complete the three demonstrated T020 lint corrections

Existing Sol-high coordinator; repository `/Users/dowwie/MyProjects/validator`.
Save full acknowledgment to
`docs/dispatches/validator-t018-t020/020-coordinator-to-owner.response.md`.

Owner read full responses018 and019. The three ordinary Clippy diagnostics are
T020 defects, not accepted baseline limitations. The required gate already makes
their correction necessary; no product or risk decision is outstanding.

Dispatch the retained evidence developer as sole Terra-high writer, via a new
prompt, to remove only the two needless `&evidence` borrows in `src/app.rs` and
the identity map in `src/model/multi_label.rs` recorded in response019. Read the
owning functions and Rust skill before editing. No staged-dead-code cleanup,
suppression, API, schema, tolerance, oracle or behavior change is authorized.

Run the original six filters and final fmt, unmodified Clippy, locked tests,
release build and diff checks on these corrected production files. Save hashes,
results and the exact residual staged inventory. Previously unchanged evidence
may be reused; the coordinator must not add another full rerun. Then freeze and
dispatch the single independent T018-T020 review as already authorized. The
reviewer must assess the full scoped candidate and original criteria, not just
these lint removals. Preserve the one finding-driven repair/recheck allowance.

Keep Card184 Working On while the writer is active, index/link this prompt and
all resulting artifacts, preserve caffeinate, and return the combined verdict
and handoff for owner acceptance before T021. Mechanical corrections necessary
for mandatory gates are ordinary completion work; do not introduce another
approval boundary for equivalent fixes within an already authorized write scope.
