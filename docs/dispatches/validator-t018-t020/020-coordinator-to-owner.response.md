# T020 mechanical-lint correction acknowledgment

I read owner prompt020 in full. The three ordinary Clippy diagnostics are required
T020 completion defects. I will use the retained prompt018 evidence developer as
the sole Terra-high writer under a new numbered dispatch to remove only:

- `&evidence` at `src/app.rs:369`;
- `&evidence` at `src/app.rs:390`;
- the identity `.map(|(label, value)| (label, value))` at
  `src/model/multi_label.rs:463`.

Before editing, the writer must read the Rust skill and owning functions. No
staged-dead-code cleanup, suppression, public API, schema, tolerance, oracle,
dependency or behavior change is authorized.

The writer will run all six exact filters plus formatting, unmodified warning-
denied Clippy, full locked tests, release build and diff check once on the corrected
production hashes. It will save exact hashes, command results and the residual
20-symbol authorized staged inventory. The coordinator will reconcile the handoff
without another suite rerun, freeze the complete T018-T020 candidate and dispatch
the single full-scope independent Sol-high review immediately.

The review retains owner001's one finding-driven repair/recheck allowance and must
assess all original T018-T020 criteria, schemas, real APIs, evidence and prohibited
shortcuts. No T021 work begins before the owner accepts that combined verdict.
Card184 returns to Working On while the writer is active, the caffeinate hold
remains active and all artifacts will be indexed/linked.
