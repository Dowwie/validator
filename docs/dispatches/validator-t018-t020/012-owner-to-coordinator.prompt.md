# T020 completion split at the observed failure boundary

Existing coordinator: Sol-high. Repository `/Users/dowwie/MyProjects/validator`.
Save full acknowledgment to
`docs/dispatches/validator-t018-t020/012-coordinator-to-owner.response.md`.
Follow manage-dev-team and file-based dispatch rules. No coordinator code/test edits.

The owner read complete responses010 and011 and inspected the current report
schema. Response011 SHA:
`152052824b58dfe769987c4135de6d27460b2e7d2ac70b4ee2e85f93a0d08a1f`.
No candidate is accepted. All existing owner001/006/009 criteria remain in force.

## Reassessment

The fresh writer changed schemas but again stopped before finishing even that
part. The current report schema still permits arbitrary hard/probability/signals
fields and unconstrained episode objects; the missing population_unit assertion
is an example, not the whole defect. The assignment combined schema reconstruction
with several independent test families. Another unchanged full-bundle retry is
not justified. Split completion into the following locally verifiable outputs,
with one writer at a time and automatic advancement on complete evidence.

1. **Report schema only.** Resume the existing replacement010 Terra-high worker
   in its retained context with a new file-based prompt limited to completing
   `schemas/v2/report.schema.json` and focused schema assertions in the existing
   conformance tests. Do not give it the full hard/marginal/CLI completion bundle.
   It already read the typed serialized report and is best placed to finish this
   narrow artifact without another full orientation. Require every concrete
   single/multi hard, probability, signal and episode structure; shared source,
   preparation, metric and identity constraints; closed nested fields; exact
   required fields/status/null branches; and task-family discrimination. Opaque
   JSON remains opaque only where the specification allows it. Preserve all
   accepted single-label constraints and actual multi-label validity. Work from
   the actual typed serialized contracts and ratified specification; use an
   accepted earlier schema only if its bytes are actually available and its hash
   matches. A hash alone is not a recoverable source file. Do not invent a restored
   baseline or stop for lack of the old file.
   Run the existing single_report_schema negative test plus real single/multi
   positive documents and focused invalid mutations proving closed families,
   required nested fields and status/value combinations. This local handoff needs
   hashes and a complete schema-to-serialized-contract mapping, not full release
   gates or numerical-oracle work. No broad reformatting or production changes.
2. **Other two schemas.** After reading that complete handoff, use one fresh
   Terra-high/fork-none writer to finish only the current check and inspection
   schemas and their original focused discrimination evidence. Preserve the
   completed report schema. Inspect their present constraints before editing;
   make no changes when already correct. Local return: both real command outputs
   validate, relevant forbidden/missing fields reject, original strict opaque-
   value semantics remain intact, hashes and criterion evidence saved. No scoring,
   policy, comparison or extra audit work.
3. **Numerical and CLI evidence.** After those local handoffs, one fresh Terra-high/
   fork-none writer completes exactly the hard oracle, true equal-count pair,
   marginal endpoint/scaling/bins and remaining CLI assertions in owner009. Now
   the schemas are satisfied prerequisites, not another unfinished assignment.
   It runs the six original nonzero filters and full required boundary commands,
   with owner001/006 staged-warning treatment. Minimal production corrections
   require a reproduced existing-contract defect; no unrelated refactor.

Each dispatch is brief and scoped to its immediate output, linking shared
authority rather than requiring reconstruction of the whole attempt history.
Preserve existing source hashes and valid evidence. This split does not create
new product tasks or reset failure history; keep the work on Card184. The
remaining value is material because strict schemas and numerical evidence are
mandatory product requirements, and these smaller handoffs isolate the observed
delivery difficulty. If a worker returns incomplete again, save exactly why it
stopped (including an actual runtime limit if observable, never guess), its last
completed result and smallest unfinished item, then return for owner reassessment.

No review is authorized before all three outputs and the full T020 boundary are
complete. The one combined independent T018-T020 review and its one finding-driven
repair/recheck allowance remain unchanged and unopened. Index all new dispatches
and responses, link them in Fizzy, verify rendered formatting/first incomplete
step, keep Card184 Working On while the worker is active, and preserve caffeinate.
Return to the owner after combined review before T021.
