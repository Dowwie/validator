# T020 stored-inspection repair acknowledgment

I read owner prompt015 in full. I accept the reproduced `E_IO` as a T020
generalized-inspection regression, not a schema finding or optional extension.
The active prompt014 schema writer remains the sole implementation writer and will
continue in its current context.

I will issue a numbered scope extension limited to `src/app.rs` and the relevant
existing CLI/conformance regressions. Before editing, the writer must read the
Rust best-practices skill, both inspection rebuild branches and the existing
`StoredRun::verify_evidence` interface. Both task branches will obtain contained,
verified evidence bindings through that existing stored-run interface and rebuild
from them. Original evidence paths remain provenance. No duplicated I/O, fallback,
format change, weakened equality/error/tolerance, comparison/policy/dependency or
public export is authorized.

The preserved failing case is relocated single-label inspection after original
inputs/evidence are unavailable. The multi-label real command case will add bound
evidence, relocate the run, pass inspection from the contained copy, then reject
stored-evidence tampering through the existing precise error contract. Existing
binding/tamper assertions will be reused. Cargo commands will run serially.

The developer will save a repair-specific response016 and finish the original
schema handoff014 without ending the active assignment early. Both responses must
record the root cause, changed paths/hashes, failed/passed commands, schema mapping
and unchanged completed report schema/other production files.

After local reconciliation I will advance automatically to owner012's fresh
numerical/CLI evidence stage. Any independent scope gap or incomplete return goes
back to the owner. The combined T018-T020 review remains unopened, Card184 remains
Working On and the caffeinate hold remains active.
