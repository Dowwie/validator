# Correct T020 staged-lint interpretation

Role/model: existing fresh sole T020 implementation developer,
`gpt-5.6-terra`, high reasoning, `fork_turns: none`. Do not delegate.

Repository: `/Users/dowwie/MyProjects/validator`.
Governing assignment: `005-coordinator-to-t020-developer.prompt.md`.

Required acknowledgment response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t018-t020/006-t020-developer-to-coordinator.response.md`.

Save a concise substantive acknowledgment there before the final T020 handoff.
Keep the full implementation handoff at the existing required response005 path.
Do not restart orientation or implementation.

Owner prompt001 remains authoritative and unchanged. Its lint rule supersedes the
narrower final wording in prompt005:

- Existing precisely staged private symbols from the accepted single-label
  backbone and T018/T019 remain allowed under their already recorded later-task
  mappings. T020's actual consumers should naturally reduce that inventory, but
  T020 is not a cleanup gate for every inherited staged diagnostic.
- Only a **new** unavoidable private policy/DTO member introduced by T020 needs an
  exact real T021 or T022 consumer named before it can be staged.
- No ordinary/new warning class is allowed. Do not suppress diagnostics, add fake
  references, widen visibility, remove required contracts, invent speculative
  consumers, or treat an unchanged authorized diagnostic as an artificial blocker.
- Preserve the warning-free T027/T035 gates and report the exact final residual
  diagnostic inventory and the existing/new distinction in response005.

All implementation scope, six-filter evidence, full boundary gates, one combined
review, single-writer boundary and T021/T022 prohibitions from prompt005 remain
unchanged. Continue from current progress.
