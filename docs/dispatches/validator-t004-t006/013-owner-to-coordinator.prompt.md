# Owner reassessment: complete the bounded repair with a fresh developer

Role/model: existing Sol-high coordinator, retained context.
Repository: `/Users/dowwie/MyProjects/validator`.
Required response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t004-t006/013-coordinator-to-owner.response.md`.

## Cause, changed approach, and value

The owner read verifier007, repair010, both incomplete developer responses, and
escalation012. The observable failure is premature handoff: twice the worker
ended without completing authorized corrections and without a technical blocker.
The second handoff also omitted a changed file from its list. There is no evidence
that the specification or required repair is infeasible. Reissuing the entire
same assignment again is not the next approach.

Retire the current developer from this build and authorize a replacement
**gpt-5.6-terra, high, fork_turns=none** as the sole implementation writer. Do not
send further work to the retired handle. Keep the existing coordinator and
independent Sol-high verifier; this replaces the developer role and does not add
another concurrent writer or role. If the runtime cannot provision the replacement
within the concurrency limit, report that capability limit before dependent work.

Provide a fresh file-based brief with exact authoritative source sections, the
current partial source hashes from a reconciled baseline, the two findings, and
the bounded write scope from prompt010. Include the manage-dev-team skill, role,
Rust skills, project instructions, all existing preservation tests, and mandatory
saved-response path. Do not make the replacement reconstruct the entire dispatch
history; the specification, findings007, partial-state escalation012, and current
repair scope are sufficient. Previous accepted source is retained, not rebuilt.

The additional investment is warranted: these two corrections are necessary for
the input integrity and checked-model contract on which all scoring depends. The
bounded result is the completed original repair, not a new feature or audit.

## Two concrete completion units

1. **Complete the numeric boundary correction and its proof.** Finish actual-entry
   `large_number_boundary` coverage for opaque input/source/preparation `1e400`
   retention and typed scalar E_OBSERVATION. All legal numeric and tagged shapes,
   marker-object rejection/preservation, null distinction, exact large integers,
   original bytes and recursive duplicates must still work. Run the affected
   existing T002/T004/T005/T006 filters, not only the negative marker test: a path
   that rejects all numeric fields cannot pass as a repair. Existing partial code
   is unreviewed and may be adjusted if it does not meet these criteria. Send a
   concise milestone to you when these actual checks pass; no independent review
   or acceptance occurs at this internal point.
2. **Complete the checked configuration/population/evaluation boundary.** Implement
   the required checked EvaluationConfig<Policy>, retained dataset digest and
   exact population counts/partition, configuration validation before selection,
   and fallible/sealed aligned evaluation construction from finding007. Extend
   population_alignment and the owning negative-construction test, preserving all
   existing semantic checks. Then run the complete named filters and combined
   fmt/test/build/Clippy checks from prompt010 and save one complete handoff.

Ordinary local implementation corrections within these two units are authorized.
Do not return a final response at orientation, after one changed field, or after
only one test. If blocked, report the exact command/contract/source issue and
smallest needed decision while completing independent remaining work where useful.
Reassess promptly on another incomplete return, or after approximately 15 minutes
without a concrete milestone; do not silently repeat the same assignment.

The coordinator must reconcile the complete changed-file set and commands before
freezing. One independent focused recheck covers the completed original repair
and justified regressions, reusing unchanged evidence. This owner decision does
not reset the prior repair allowance or authorize unbounded retries. Any failed
recheck or repeated incomplete handoff returns to the owner with its cause.

## Continuity

No T007+ work, new component, public API, parser framework, relaxed criterion,
suppression, duplicate implementation, or additional verification team is added.
Preserve task-owned caffeinate PID84732/session29011 and the existing user build
authorization. Keep card183 Working On, record the retired/replacement handles
and current partial state, index/link all new prompt/response artifacts, and save
your acknowledgment before continuing active coordination.
