# Accept checked admission and dispatch the check/evaluate sequence

Role/model: existing Sol-high coordinator, retained context.
Required response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t004-t006/027-coordinator-to-owner.response.md`.

## Owner acceptance

The owner read the complete repair evidence, verifier025 Ready response and
coordinator026 handoff. Accept the T004–T006 checked-admission candidate, including
its incorporated T002 wire corrections, at final manifest SHA-256
`9e9b953930d1d8d334375f3a0ca4f0a22aed5f91eb8cb88cabb32cddc6c1107d`.
The accepted verifier response SHA-256 is
`f5214f9d3030cd439e6ee05ae0924bffdc005b360b66feaeab867a0eb39e1415`.

The strict wire/opaque boundary, checked source/observation semantics, single-label
scoring admission, complete configuration/population identity, alignment, and
omitted-selection constructor invariant now have independent supporting evidence.
No further admission review is needed absent a changed artifact or concrete new
defect. The 24-test suite and focused filters pass.

This is intermediate acceptance. Clippy still exits 101 with the explicitly
disclosed 107 production and 19 lib-test incomplete-consumer dead_code groups; it
is not passed or waived. The complete single-label CLI and steel-thread outcomes
remain unmet, with clean full Clippy required at their integration gates.

## Next bounded outcome

Now dispatch the exact **T007 -> T008 -> T009 -> T010 -> T011 -> T012 -> T013 ->
T014** sequence authorized in build dispatch004. Reuse
`/root/coordinator/replacement_developer` as the sole developer. Keep the retired
developer inactive. Preserve the existing independent verifier for the combined
frozen review.

The required outcome is real single-label `check` and `evaluate` through the small
application API and actual CLI, with correct metrics/statuses/signals, strict
input/output schemas, exact source snapshots/evidence bindings, non-replacing
publication, typed reports and receipts, and safe machine-readable errors.
Dispatch every exact task contract and normative source reference, not a phase
title. Parent-module and minimal dependency wiring are already allowed.

Honor the construction rule in build004: private local behavior receives owning
unit tests as it is implemented; required conformance tests use the real T014
application API when it exists. No test-only facade, public internals, included
production source, placeholder command, or fake nonzero test is acceptable.
Preserve all admission regressions and required independent numerical oracles.

Request concise evidence milestones after pure scoring and after artifact
publication. Run compile checks at coherent points. All original named tests and
the full pinned fmt/Clippy/test/release-build gates must pass before handing the
combined candidate to the verifier. Reassess a concrete integration constraint
instead of suppressing, faking a caller, or quietly weakening a check.

The combined checkpoint gets one coordinator-managed criterion-bound repair and
recheck before owner reassessment. A repeated incomplete developer handoff is
also an immediate owner reassessment boundary. No T015+, multi-label expansion,
private acceptance-data preparation, unrelated audit, extra writer, or external
operation is added by this dispatch. T015–T017 remain the next steel-thread gate.

## Evidence and continuity

Use machine-generated hashes in the frozen manifest. Worker responses can link
that manifest for unchanged file hashes and list only changed hashes instead of
retyping long identical tables; this avoids the repeated harmless lib.rs hash
transcription typo. Preserve substantive criterion/test evidence and immutable
history. The verifier binds its verdict to the complete manifest as before.

Update card183 with this acceptance and actual next action, verify rendered
formatting/first incomplete step, index/link prompt/response/new dispatch, and run
the plan/index check. Preserve caffeinate PID84732/session29011 through continuing
authorized work. Save the required acknowledgment and continue active supervision.
