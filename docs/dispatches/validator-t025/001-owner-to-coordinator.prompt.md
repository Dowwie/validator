# Accept schema contracts and verify numerical conformance independently

Existing coordinator: `gpt-5.6-sol`, high reasoning. Repository:
`/Users/dowwie/MyProjects/validator`. Follow manage-dev-team and file-based
dispatch rules; no implementation, test or verification-code authorship. Save
full acknowledgment to
`docs/dispatches/validator-t025/001-coordinator-to-owner.response.md`.

## Acceptance and bounded outcome

Owner read full T024 verdict006, repaired Ready009 and coordinator handoff010.
Accept the exact repaired schema/case candidate:

- manifest008 `91d756bbb4253a832461c2e3642a585c00286caf471b3776abf5af8325b5e0d3`;
- Ready009 `b26aafa786843511a492bd7ee47aa1f58a06299b3431aa4227b09120c42eb543`;
- handoff010 `e4ee78a0c081d8289a9f6b2491bb09e944d8ce3af50c78a9bec7685e531dee09`.

The nine schemas, two umbrella filters, fourteen assigned compound cases and
reported schema mutants/legal neighbors are verified. All 90 tests, format and
release pass. The exact staged Clippy 17 production + 3 duplicate lib-test limit
persists only until T027; full-product acceptance remains unfulfilled. Record
acceptance and complete Card186. Search before creating/reusing the next General
card, linked to Card186, for **T025 only**. Do not begin T026+ before owner
acceptance of this next checkpoint.

## Role and independence

Read complete T025, its normative sections and assigned coverage rows. As for
T024, resolve historical implementation-role metadata to the actual governing
split: fresh Terra-high/fork-none sole developer authors the permanent independent
oracles/tests, and a separate fresh Sol-high/fork-none read-only verifier judges
the frozen result. Update the execution/task role mapping without changing any
product criterion. Coordinator owns acceptance-record assembly, index and tracking.

The numerical oracle author must derive expected results from the ratified
mathematics and raw synthetic cases, not read/copy `src/evaluation*` or production
metric/count helpers, invoke production to generate expected numbers, or tune
fixtures to observed outputs. It may read published schemas/API contracts and
existing test setup/assertion helpers to call the real application and preserve
accepted evidence. Expected calculation code must remain independent of production
scoring. A test calls production only to obtain the actual value being checked.
No private sources, inference, service, external reference library, dependency or
generic oracle framework is needed.

## Concrete work and economical local handoffs

Own T025's `tests/conformance.rs` and the existing single-/multi-label expected
fixtures. Preserve every accepted fixture value and assertion; extend without
rewriting history. Fresh fixtures under those existing task-owned directories may
be added only when necessary for an assigned case and must be indexed. Production
and schema edits are not part of oracle construction: a demonstrated discrepancy
must be saved with expected derivation, actual output, exact input and owning
location, then returned to the owner for a bounded correction decision. Never
change expected values or tolerances to erase that discrepancy.

Avoid the prior dense-bundle failure pattern. Use these complete local outputs,
one writer at a time, fresh context for each substantial next output, advancing
automatically after a complete saved handoff:

1. Independent expected fixtures/derivations and the two exhaustive filters.
   Enumerate every expected/predicted subset pair of a three-label vocabulary
   (64 pairs) plus the eight whole-abstention reference cases. For single-label,
   enumerate all class/decision combinations over three classes plus typed
   abstention for populations of lengths 0, 1 and 2; include record-order
   permutations. This is a bounded exhaustive tranche, not a fuzzing campaign.
   Preserve and independently reconstruct F04 (accuracy 5/8, macro-F1 131/210)
   using its established source/fixture, without importing the master spec's wider
   features or its superseded output conventions.
2. Complete the exact assigned single-label S01-S12 and evidence E01-E04/E07
   numerical filters, each with every subcase from its normative row.
3. Complete the exact assigned multi-label M01-M15/M20 filters and
   `full_numeric_conformance`, then final T025 gates. The umbrella must not
   substitute for any named row; it can reuse the same independently derived
   scenarios rather than duplicating decision logic.

If a smaller different grouping is clearly more efficient, keep the same bounded
outputs and one-writer/independent-review boundaries; do not create new product
tasks. No additional owner gate is needed between complete local outputs.

## Required numerical evidence

The complete task, not this summary, governs. In particular prove:

- Single-label matrix dimensions, typed abstention column, supports and every
  accounting identity; one off-diagonal decision is one error. Correct rates,
  selective populations, per-class precision/recall/direct F1/coverage and fixed
  macro zero-fill/status. Distinguish observed-but-always-missed from absent class.
- Multi-label N/U/G/D/E, answered-only binary TP/FP/FN/TN, total reference support
  versus answered support, each four-count sum G and total G*K, exact-set versus
  binary results, named micro/macro/Hamming ratios and populations. Empty answered
  set, whole abstention, missing row and invalid set remain distinct. Verify the
  specified null set differences and explicit abstention status, including the
  contract's `status: "abstained"` requirement, against the actual public evidence.
- Applicability before empty-population status; no_data, no_answered_predictions,
  undefined_zero_denominator, contains_undefined_classes, not_applicable and
  positive_infinity with exact null/special-value forms. Verify units/scopes/counts
  and numerator/denominator where specified, not merely headline values.
- Independent rational Brier calculations, analytical natural-log expectations,
  observed-branch endpoint infinity/zero, categorical .08 versus marginal .04,
  unnormalized marginal .265 example, and F04. No clipping or normalization beyond
  the fixed categorical admission rule. No multi-label argmax/ECE/joint likelihood.
- Exact binary64 threshold and bin boundaries (below/equal/above, including 1),
  schema-order ties, recorded-choice versus diagnostic argmax, confidence versus
  maximum probability, and original versus working values where applicable.
- Raw/final separation and unchanged probability populations under policies;
  probability-bearing abstentions, raw-answer confidence populations/IDs, explicit
  abstention preconditions, no observation promotion, and the .99 scoring rejection.
- Compound invalid sets/keys/task/source/policy cases reject before filtering or
  scoring. Reordered records/sets/maps preserve calculations and ordered evidence
  while exact byte digests may change.

Each assigned filter must call the real public path, list on the exact target and
execute nonzero. Use exact integer/rational checks where representable and only the
existing ratified float fixture tolerance where needed. Do not widen a threshold,
bin, normalization or replay tolerance. Reuse previously verified behavior where
appropriate, while making each assigned compound row's evidence explicit.

## Freeze, verification and return

Local outputs need focused results and complete saved criterion/derivation/hash
handoffs. Final output runs the exact full T025 filter inventory plus pinned fmt,
unmodified warning-denied Clippy, full locked tests, locked release and diff checks.
No new/ordinary warnings; the existing exact 17+3 staged inventory remains a stated
limit, with completely clean T027/T035 gates unchanged. No fake uses, suppressions
or visibility expansion. Reuse target directories and serialize Cargo commands.

Freeze exact files and dispatch the independent Sol-high verifier for T025 only.
It must independently derive the mathematical expectations, inspect the oracle's
independence and all assigned compound rows, and run the bounded discriminating
checks. Prior schema/publication/CLI proofs may be reused; do not reopen T024 or
start T026/T027/private acceptance. Stop when the scoped verdict is supported.
Findings require a criterion, location, reproduction, consequence and smallest
correction. A discrepancy in public wire evidence is real if the literal normative
contract is violated; do not silently substitute current implementation spelling.

One finding-driven coordinator repair/recheck cycle within owned test/fixture
scope is authorized. Required production or schema corrections come to the owner
with evidence and proposed scope before editing. Repeated incomplete work also
returns with the actual cause and changed bounded approach, not a repeated bundle.
Do not introduce another approval pause for routine corrections inside owned files.

Keep Fizzy's first incomplete step and rendered notes current; index all dispatches,
responses and accepted fixtures; maintain task/physical mappings, plan/index links
and diff checks. Preserve unrelated/untracked work and the current caffeinate hold.
No staging, commit, cleanup, deployment or data-source mutation. Return the complete
verdict and coordinator handoff for owner acceptance before T026.
