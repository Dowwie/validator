# T025 abstention-boundary repair acknowledgment

I read owner prompt004 in full. The literal `status: "abstained"` and null
matched/missed/extra fields are a product serialization/schema requirement, not an
oracle choice. I will preserve the inactive oracle writer's context, direct
derivations, expected fixtures and assertions unchanged.

I will provision one fresh Terra-high/fork-none sole repair writer for only
`src/model/multi_label.rs`, report/inspection schema synchronization and focused
conformance/CLI evidence. It will preserve counts, scores, probabilities,
observations, policies, answered outcomes and single-label wire shapes. It will
test field presence explicitly for report and verified inspection, with and
without reason and with probability/observation evidence where present.

The repair also owns the exact literal `ABSTAIN` class reproduction. A declared
class named `ABSTAIN` must remain legal and distinct from the structurally typed
abstention column; if reproduced, only the name prohibition is removed while the
typed-column requirement and negative remain.

I updated T025's task/physical/downstream input maps for the necessary production,
report/inspection schema and focused CLI ownership without changing criteria.
After a complete correction handoff I will reactivate the preserved original
oracle developer to rerun and finish output 1, then continue owner001's remaining
fresh local outputs. T026+ remain paused and the final independent T025 review has
not begun.
