# T025 per-label population-unit correction acknowledgment

I read owner prompt016 in full. I will preserve the current repaired candidate
until verifier015 saves its bounded finding that the public M02 result and repaired
test use `episode` while the ratified multi-label per-label unit is
`label_decision`. The active verifier has been notified of this exact concern and
will not expand its recheck.

The fixed expectation is population count `G`, unit `label_decision`, scope
`answered` for multi-label per-label precision, recall and F1, including
status-only cases. Aggregate binary metrics retain `G*K`; set-level metrics retain
episode units. Ratios, values, statuses, counts, fixtures and tolerances are
unchanged.

After the saved Revise finding, I will resume the retained numeric developer as
sole writer in a new file-based dispatch. Its bounded write scope is
`src/evaluation/multi_label.rs` plus focused `tests/conformance.rs`/owning tests.
It will reuse the existing `answered_label_metric` with population `G`; no new
helper, formula, schema, fixture, policy, probability or unrelated behavior is
authorized. Required evidence will cover normal, undefined empty-set,
all-abstained and empty-selection raw/final per-label metrics, preserve aggregate
and set-level units, and include one real replay/inspection or comparison path.

I updated T025's artifact ownership, the physical map and T027's T025 input list
for this necessary evaluator correction without changing product criteria. The
same verifier will receive one newly frozen candidate for a focused unit recheck,
reusing all unaffected T025 evidence. One further concrete discrepancy returns to
the owner. T026 remains undispatched pending owner acceptance.
