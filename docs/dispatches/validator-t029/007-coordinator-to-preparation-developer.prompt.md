# Bind both routes' raw native evidence in T029 preparation

Resume the retained clean preparation developer as sole writer. Required repair
response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t029/007-preparation-developer-to-coordinator.response.md`.

Read verdict006 SHA-256
`daa33712402877c8a0b6ab1102f5bd087fe081ae5d343e49ac3cbbcb9a8eaa13`.
Correct only F1. Deterministically append each route's 630 accepted
`native_route_raw_response` protected paths to that source's evidence array,
referencing existing frozen sibling files without copying them. Include every
Score raw-response evidence index in `preparation.evidence_indices` alongside the
existing script, manifest/map and receipt bindings. Preserve stable order and
exact hashes/roles.

Regenerate the affected full predictions, six route subsets, copied preparation
script and receipt. Golden/configs, mappings, observations, selections, outcomes
and product code remain unchanged. Repeat two-directory byte equality, all 15
hashes, 0700/0600, source/map stability, public script checks and eight installed-
binary `check` invocations. Confirm 630 direct raw bindings per route and every
Score raw index resolves correctly.

Do not read oracle/T030, evaluate performance, add tooling/cases or make unrelated
changes. Save old/new hashes and focused evidence in response007, then stop for
the same verifier's recheck.
