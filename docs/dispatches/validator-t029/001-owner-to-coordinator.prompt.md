# Prepare the frozen native routes reproducibly

Existing Sol-high coordinator in `/Users/dowwie/MyProjects/validator`.
Save acknowledgment to
`docs/dispatches/validator-t029/001-coordinator-to-owner.response.md`.

## Start boundary and outcome

T029 is conditionally authorized once T027 is owner-accepted and the T030 oracle
author has saved its complete frozen implementation handoff. T030 independent
review may run alongside this sole developer. If that review needs a repair, let
the preparation author save its current small coherent checkpoint and yield the
writer to the oracle's retained author. Resume from the checkpoint afterward.
There is no authority to consume a partial oracle or declare either task accepted.

Assign a fresh Terra-high/fork-none sole developer with python-dev. No worker
delegation, no second writer, and one independent verifier at a time. The outcome
is complete T029: deterministic canonical native Choice and Score artifacts,
full inputs, observations, selections and preparation evidence from the exact
accepted T028 protected bundle. Read complete T029 and its normative specification
sections, source/observation/preparation wire contracts, and published schemas.

The accepted source manifest hash is
`860300bc288ad1f46c98631f78ad353066d7798a54c182ac5b60d8a1b693444e`;
the persisted UUID map hash is
`a987df05727164c382f0ea404bd4f465aa9dbaf7fb3f6e8c74d5bedac5cd32b4`.
Acceptance is externally recorded in T028 owner006; immutable embedded creation
status must not be rewritten. Read only frozen sources, never moving Chord trees.

## Sole developer scope

Own `scripts/acceptance/prepare_chord.py`, minimal directly relevant public
synthetic script tests, and the exact protected T029 outputs. Use existing Python
tooling where available; do not add a packaging/framework project for this script.
Coordinator owns shared docs, ownership maps, artifact index and Fizzy. No Rust,
schema, oracle or frozen source/map edits. Never derive a transformation from
expected metrics; do not read/import `oracle_chord.py` or its expected results to
implement native decoding, mappings or observation preservation.

Preserve 603 complete intended classifier-visible episode inputs and the 27
original withheld/unscored records and reasons in the receipt. Separate reference
judgments/review notes from classifier-visible payload. If saved source contexts
create a material ambiguity about the common gold input, report the exact shape
and smallest decision before inventing a lossy selection.

Decode all recoverable native outcomes, including historical malformed parsed
projections. Missing native outcomes are a failure, not abstention or filtering.
Choice uses its recorded class; Score uses the frozen scalar boundaries/ties and
retains UNCERTAIN even when distribution argmax differs. Sources are classifier
and retain actual model/question/prompt/configuration differences. Preserve native
scalar, distribution, confidence where available and all three auxiliary Bernoulli
observations with complete definitions. No numerical repair or normalization.

Omit scoring probabilities and scoring confidence on every row; retain reported
numerical values as observations. The preparation receipt identifies the omitted
scoring families, reasons and affected IDs. Attach the required Score preparation
descriptor with exact mapping/version/configuration and indices that actually
bind the script, raw native source artifacts and receipt. Source evidence can
reference the accepted protected raw files; avoid unnecessary copies of the whole
277 MB bundle. Report any unrepresentable native observation under the published
contract rather than silently coercing, dropping, or inventing a fallback.

Implement the prescribed `--bundle ... --out ...` and `--selection <config>`
interfaces. Emit all canonical T029 artifacts, including full gold, both full
prediction artifacts, full/representative/challenge/targeted configs, both routes'
three exactly aligned subset artifacts, and preparation receipt. Every subset
remains bound to the exact whole golden bytes. The targeted example uses the
owner's pre-measurement rule from T030 owner001: first 50 labeled source IDs in
ascending exact string order, mapped through the unchanged UUID map. It is a
partial-population demonstration, not a representative or optimized sample.

Keep canonical bytes independent of wall clock, fresh output-directory name and
dictionary iteration accidents. Preserve original native numbers and provenance.
Evidence paths and receipt hashes must not create a circular digest dependency.
Use fresh protected output directories for proof; final delivery may add only the
new named canonical files to the existing protected bundle, without overwriting
its source metadata, map, raw files or independent expected result. Keep private
directories/files at 0700/0600 and private payloads out of public logs/fixtures.

## Verification and handoff

Run two identical preparations in separate fresh private directories and compare
exact bytes/hashes of every canonical output and receipt. Prove frozen sources and
map remain unchanged. Exercise both cohort selections and targeted selection,
check exact selected ID alignment and full-gold bindings, and prove observations,
source differences, derived-route ties and all exclusions against frozen evidence.
Use focused public synthetic tests for consequential parsing/mapping/selection
failures. Script syntax/lint/type checks should use provisioned appropriate tools;
do not bypass a failing configured check or bootstrap unrelated tooling.

The accepted T027 executable may check canonical shapes/alignment. Do not run
performance evaluation or compare with expected metrics before the independent
T030 oracle is owner-accepted. Public automated tests must have no private-data
dependency. Source copying, script outputs and check results are preparation
evidence, not T033 fresh operator acceptance.

Freeze the complete script/test/output candidate and save a substantive response
with hashes, every criterion's actual evidence, exact commands/exits, source
stability and remaining limitations. When the verifier role is free after T030,
use a fresh Sol-high/fork-none read-only verifier to reproduce preparation and
inspect the required boundaries against raw inputs and contracts. Do not let this
review redefine mappings or fit observations to scoring. One bounded
finding-driven repair/recheck is authorized; repeated incompletion returns to
owner with the cause and a changed bounded proposal.

Search/update the General-board card and maintain rendered notes and first
incomplete step. Index the script, tests where governing, protected canonical
inputs/receipt and dispatches; reconcile source/physical ownership and required
link/plan/diff checks. Keep caffeinate PID84732 active. Return exact independent
verdict and full handoff for owner acceptance. T031+ remain undispatched.
