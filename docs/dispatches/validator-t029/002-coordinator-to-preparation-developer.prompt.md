# Prepare the frozen Chord630 native routes reproducibly

Role/model: fresh sole developer, `gpt-5.6-terra`, high reasoning,
`fork_turns: none`. Do not delegate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #192 — Prepare Validator Chord630 native artifacts](http://localhost:3006/1/cards/192).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t029/002-preparation-developer-to-coordinator.response.md`.

Save the full handoff before returning only path, SHA-256 and status. You are the
sole writer while T030 has a read-only verifier. Apply python-dev. Do not delegate.

Read global/repository AGENTS, python-dev, complete T029, owner prompt001 and its
normative dataset-preparation/source-observation sections. Read only accepted T028
protected sources/manifest/map. Do **not** read/import `oracle_chord.py`, protected
`expected.json`, T030 responses/results or prior performance metrics. Do not use
moving Chord trees.

Accepted inputs remain manifest
`860300bc288ad1f46c98631f78ad353066d7798a54c182ac5b60d8a1b693444e`
and map `a987df05727164c382f0ea404bd4f465aa9dbaf7fb3f6e8c74d5bedac5cd32b4`.

Own only `scripts/acceptance/prepare_chord.py`, minimal consequential public
synthetic script tests if needed, the exact protected artifacts listed in T029,
and response002. No Rust/schema/oracle/frozen source-map/governance edits.

Implement the exact `--bundle ... --out ...` and `--selection <config>` interface.
Preserve all 603 classifier-visible inputs, both complete native outcomes, the 27
excluded records/reasons in the receipt, actual classifier source/model/question/
prompt/config differences, native scalar/distribution/confidence values and all
three auxiliary Bernoulli observations with definitions. Score outcomes use the
frozen boundaries/ties and keep UNCERTAIN even on argmax disagreement. Retain
numbers as observations; omit scoring probabilities/confidence on every row and
record reason/affected IDs. Attach the required Score PreparationDescriptor with
actual script/raw/receipt indices and exact mapping/version/configuration.

Emit full golden, full Choice/Score predictions, full/random400/supplements230/
targeted configs, exactly aligned route subsets for the three selections, and the
receipt. All subsets bind the exact whole golden bytes. Targeted is fixed: first
50 labeled source IDs by ascending exact string order, mapped through persisted
UUIDs. Do not optimize or use performance results.

Run twice in separate new private directories and require byte-identical outputs/
receipts. Prove source/map unchanged, 0700/0600 modes, selection alignment, gold
bindings, observations/source differences/ties/exclusions. Use only minimal focused
synthetic failure checks. The accepted T027 binary may `check` shapes/alignment;
do not evaluate performance or compare to oracle expected values.

Keep the frozen-case script minimal; no framework, database, generic tooling,
documentation polish or optional coverage. If a common-gold input ambiguity or
unrepresentable required observation appears, freeze exact shape and stop rather
than coercing. Response002 records all hashes, criteria, commands/exits,
reproducibility, source stability and limitations without private payloads. Do not
claim acceptance or start T031.
