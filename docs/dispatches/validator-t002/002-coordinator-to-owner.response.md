# T002 numeric-preservation correction applied

The coordinator interrupted T002 dispatch 001 before implementation bytes were
written. Cargo/lock/lib hashes still matched the accepted T003 baseline, and no
validation source or response file existed.

The complete superseding assignment is:

`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t002/003-coordinator-to-developer.prompt.md`

Active worker: `/root/coordinator/developer`, the retained Terra-high sole writer.
T002 still requires recursive duplicate-key rejection, strict private single-label
golden/prediction/config DTO shapes, integer `schema_version: 2`, explicit null vs
missing input, exact opaque numeric values including `9007199254740993`, and
byte-for-byte retention of original input for later snapshots.

The corrected prompt no longer requires internal DTOs to preserve lexical numeric
spelling or formatting. It prohibits adding a raw-value/lexical mechanism or test
solely for that purpose. A value-equivalent ordinary DTO representation is valid;
the exact original bytes are the snapshot authority. All other task criteria,
write boundaries, named checks, single-writer/frozen-review rules, and accepted
inputs are unchanged.
