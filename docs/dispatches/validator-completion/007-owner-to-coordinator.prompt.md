# Install the accepted executable now

The accepted T027 Rust candidate is frozen and already passes its required gates.
Installation does not depend on the unfinished acceptance datasets. Execute the
installation portion of T032 now alongside the independent Python work; do not
wait for the combined-input owner boundary merely to produce the executable.

As an authorized integration operation, choose an unused stable user-owned root
under `/Users/dowwie/.local/share/validator/install/`, then run the prescribed
`cargo install --path . --locked --root <isolated-root>`. Record the exact path,
SHA-256, version output, command/exit and accepted source identity. Do not replace
an unrelated existing installation or modify source. Use the existing target and
serialize any Cargo operation that would contend with it.

This installation is available for eventual documented/fresh-agent acceptance;
it does not claim T032 or full DoD complete. Later T032 work reuses this exact
installation if its source identity remains current, tests the actual documented
workflows and records all required evidence. No extra independent installation
review or second install is needed merely because it happened earlier. Keep the
rest of completion002's independent-input and owner boundaries unchanged.

Index the installed executable as an operator entry point and record this evidence
in the existing acceptance record at the normal handoff. Return its concrete path
and version by message; no separate acknowledgment artifact or task is needed.
