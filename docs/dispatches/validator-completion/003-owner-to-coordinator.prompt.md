# Essential completion only

The user directs immediate completion and asks us to exclude nice-to-have work.
Apply this restriction now to completion002's remaining authorized sequence.

Freeze feature and architecture work. The remaining deliverable is only:
current candidate review; independently checked real-data and minimal multi-label
operator workflows; installed executable with the minimum usable commands; final
required checks and completion record. Only a demonstrated failure of those
required outcomes may reopen implementation. An unverified required outcome must
be reported honestly, not renamed optional or silently waived.

Do not add refactoring, generalized tooling, speculative edge cases, documentation
polish, repeated audits, expanded test coverage or process machinery. Reuse
existing sufficient proof. Keep private preparation/oracle code limited to the
specific frozen case and required reproducibility. Keep the synthetic acceptance
case as small as the specified behaviors permit. Keep documentation to what the
fresh operators actually need and required limitations. Close traceability using
existing evidence links, not new narrative essays.

Tell the active reviewer to finish its one scoped verdict, not expand investigation.
Report any actual completion blocker promptly with the exact requirement and
smallest correction. Batch required tracking/index updates with substantive
handoffs. Use the existing final response path; no acknowledgment file or new
tracking item. Acknowledge by message and execute.
