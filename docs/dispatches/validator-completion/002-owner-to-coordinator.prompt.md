# Cross the defined delivery finish line

Existing Sol-high coordinator, repository `/Users/dowwie/MyProjects/validator`.
This supersedes earlier instructions leaving T031+ undispatched. The user now
explicitly directs us to put the finish line in front of us and cross it. Continue
through the complete remaining sequence below, applying completion001's
proportional coordination. Acknowledge by message; use its already specified
completion response for the final substantive handoff.

## Finish line and owner decisions

Deliver an installed Validator that fresh agents can use for both task kinds,
with independently established expected results, all ratified contract/DoD gates
verified, discoverable evidence and accurate limitations. Stop after that outcome.
No new features, optional hardening, performance project or additional audit layer.

Use three substantive owner boundaries:

1. Return the current frozen T027 public build and independent verdict for owner
   acceptance. T030 may run concurrently under its existing authority.
2. Return a combined acceptance-input handoff after T030, T029 and T031 have each
   completed their required independent review. Owner reads those complete results
   and accepts the input/oracle bundle together. Individual Ready results permit
   the next authorized preparation assignment, without separate owner paperwork;
   they do not permit using Validator to derive or tune the expected results.
3. After installation, fresh operators and final required checks, return the
   complete delivery for owner acceptance and task closure.

Routine implementation and one bounded finding-driven repair/recheck per candidate
remain coordinator-owned. Escalate repeated incompletion or consequential contract
decisions with concrete evidence and a changed bounded proposal. Keep one writer,
one independent verifier, prescribed models and fresh contexts. All real worker
assignments remain file-based with complete response paths and indexed artifacts.

## Independent review scope and stopping rule

The user specifically challenges auxiliary development through verification.
Apply these bounds to the active reviewer and every remaining review:

- The deliverable is a verdict on the named frozen candidate, not new software.
  No source/test/fixture edits, maintained verification tool, extra framework,
  generalized audit, optional feature, benchmark, or speculative hardening.
- T027 review covers its changed model/validation/inspection code and test changes,
  necessary callers, the twelve DM rows, exact test inventory and listed clean/
  offline gates. Existing accepted behavior is regression evidence, not permission
  to reopen unrelated decisions or inspect the repository without a concrete need.
- Later reviews use the specific task's observable acceptance criteria and named
  inputs/outputs. Final review reconciles existing evidence and current identities;
  it does not launch another implementation or research phase.
- Each finding must cite a requirement, show reproducible evidence or an exact
  missing proof, explain the consequence and identify the smallest correction.
  Preference, possible elegance and hypothetical future needs are not findings.
- Use existing tests and direct inspection. A small temporary read-only command
  to resolve a concrete question is allowed; auxiliary maintained code is not.
  Any proposed new durable test or product edit goes to the sole developer as a
  justified required correction, never to the verifier as authorship.
- End after one complete scoped review. Permit one focused correction/recheck
  for demonstrated defects; repeated failure returns to owner rather than growing
  the assignment. Stop at Ready once all scoped criteria are evidenced. Report
  any genuinely unfinished criterion explicitly; do not invent more confidence
  work after the criteria pass.

Convey these limits by referencing this file in a message to the active reviewer;
no new acknowledgment document or separate process-compliance review is needed.

## Complete the independent acceptance inputs

Finish T030 and T029 under their existing owner briefs. T029 may begin after T027
owner acceptance and the complete frozen T030 author handoff. Its independent
review waits for the verifier to finish T030. Preparation checks are permitted;
performance evaluation waits for owner acceptance of the combined inputs. Both
authors retain their independent-input restrictions and the fixed first-50-source-
ID targeted selection. No expected value may be fitted to Validator output.

T031 is now authorized. Use a fresh Terra-high/fork-none oracle author after the
T029 writer finishes; it may overlap T029's read-only review. Read complete T031
and its normative metric/data-model sections. Own only its listed public synthetic
input/oracle/manifest artifacts and complete handoff. It must not read production
scoring, Validator outputs, or another production-derived expected fixture.
Freeze input bytes before calculation. Make the smallest practical case satisfying
the specified overlapping/empty sets, whole abstention with complete marginals,
known failure, two compatible runs and partial population overlap. Independently
derive exact counts/rational Brier, analytic log expressions, statuses, selected/
answered IDs, recovered/regressed/changed IDs and per-label transitions. The
manifest excludes its own hash; generated run outputs are not fixtures.

When the verifier is free, use a fresh Sol-high read-only reviewer to reconcile
all T031 oracle entries from frozen inputs before any Validator evaluation of that
bundle. Preserve those inputs and expected values thereafter. Return one concise
combined T029/T030/T031 owner handoff pointing to their complete author responses,
independent verdicts and exact identities. Coordinator maintains required maps,
index, acceptance evidence and Fizzy at these real handoffs.

## Install and document the verified executable

After the owner accepts the combined inputs, execute complete T032 with a fresh
Terra-high developer and independent Sol-high verifier. Own README, docs/usage,
the tested installation and required evidence; no scoring changes. Use a stable,
isolated user-owned install root, record the exact executable path/hash and tested
toolchain, and run `cargo install --path . --locked --root <isolated-root>`.
Use the exact installed binary for both public synthetic workflows. Document the
actual four-command interfaces, all published schemas, exact subset/intersection
preparation, failure discovery/inspection, strict errors, source/evidence handling,
observation versus scoring, protected snapshots and reference/population limits.
Keep README concise and put detailed operator commands in docs/usage.

T032 must include reproducible practical preparation/selection instructions so
the fresh operators do not need unpublished recipes. Do not claim calibration,
reference truth, significance or deployment certification. A complete independent
Ready on the frozen installed/documented candidate authorizes using it for the
next acceptance operations; this is advance operational authorization, not owner
acceptance of unseen evidence. Report its full handoff to owner without an idle
administrative approval cycle.

## Fresh operator demonstrations and final DoD

Run T033 and T034 with separate fresh Sol-high/fork-none agents, sequentially in
the verifier role. They are independent of the implementation, preparation and
oracle authors. Coordinator may write the complete dispatches at the plan's named
`docs/dispatches/validator-acceptance/001-owner-to-single-label.prompt.md` and
`002-owner-to-multi-label.prompt.md` paths under this owner authorization; identify
this delegation honestly. Save full responses at the specified paired paths.

Give each only installed binary identity, published docs/schemas, the relevant
frozen inputs and independently accepted expected results. The task JSON's
transitive source-code inputs are dependency lineage, not permission for operators
to read Rust, tests, oracle implementations or internal handoffs. Deterministic
domain preparation is permitted; scoring-code authorship is not. Execute all
T033/T034 operations, selections, known-failure inspection, default/intersection
comparison, strict numerical rejection, receipt hashing and oracle comparisons.
Keep complete stdout/stderr/exits/hash evidence and private payloads protected at
the task's prescribed paths. An undocumented required step is a usability failure;
route its smallest correction to the sole developer and rerun the affected path.

Then finish T035. This is closure of the existing evidence, not a new repository
audit. Read the substantive saved verdicts before claiming acceptance, reconcile
all required AC/conformance/structure/DoD mappings and current candidate identities,
and run the four literal mandated Rust commands on the delivered revision plus
required index/link/plan/diff checks and any applicable script checks. Preserve
independent expectations and all earlier required test names. Reuse unchanged
evidence where the contract permits it. No placeholder, bypass, weakened check,
unresolved criterion or unindexed accepted artifact counts as completion.

Keep source/schema/fixture/script/installed-binary/result hashes sufficient to
identify the delivered state. The untracked/unborn Git state remains disclosed;
do not stage, commit, reset or clean unrelated material. Preserve the protected
bundles and persisted map. Check disk before large acceptance runs and avoid
redundant whole-bundle copies; do not delete others' files to make room.

Return the final completion response with executable location, verified install/
operator commands, criterion-to-evidence links, all gate results, explicit limits
and exact remaining owner closure action. Fizzy closes only after verification and
owner final acceptance. Keep caffeinate PID84732 until the owner releases that
specific hold after completion. Do not start further work after the finish line.
