# Bounded recovery from developer-model capacity errors

T030 has had two Terra capacity failures and a third attempt is active. Let that
attempt finish; do not interrupt useful work. If it fails for the same capacity
reason, do not enter another retry loop.

The owner explicitly authorizes a temporary, disclosed staffing/model exception:
replace the unavailable Terra-high developer with a fresh `gpt-5.6-sol`, high
reasoning, `fork_turns: none` developer. This is an owner-selected replacement for
runtime availability, not an undisclosed substitution. The user has been informed.
The independent verifier remains a different fresh Sol-high agent; no self-review,
second writer or additional active role is authorized.

Confirm the failed author is inactive before replacement. Give the replacement
only the original source-isolated T030 brief, exact checkpoint/script/input
identities, complete saved response and required permission correction. Preserve
oracle independence. Continue from the existing checkpoint, not from scratch.
Record the actual capacity failures and replacement in the normal task handoff;
do not invent execution results for failed turns.

This exception can also be used for remaining developer assignments if Terra
remains unavailable. Use the prescribed Terra model when it is available. No
permission question, extra acknowledgment artifact or unrelated scope expansion.
