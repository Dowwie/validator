# Correct the T030 full challenge cohort

Resume the retained source-isolated oracle author as sole writer. Required repair
response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t030/007-oracle-author-to-coordinator.response.md`.

Read complete verifier verdict005 SHA-256
`5c479156f200d740314445d35581a9bd560fb82818657101eda4d66ab704d836`.
Correct only the demonstrated cohort selector defect:

- representative selects labeled `cohort == "old_random400"` (383);
- challenge selects labeled `cohort == "old_supplements230"` (220);
- emitted selection descriptions name those cohort predicates.

Regenerate protected `expected.json`. Confirm 383+220=603, the verifier-recorded
220-row challenge matrices/metrics/transitions and membership/order digests, while
every non-challenge entry remains identical. Repeat two task-owned private runs,
byte equality, 0600/0700 permissions, unchanged existing parents and root-owned
1777 `/tmp`, plus manifest/map stability and focused syntax/Ruff.

Do not change other oracle logic, metrics, failure selection, output structure,
permissions framework or any T029/product file. Save exact old/new script/output
hashes and focused evidence in response007, then stop for recheck.
