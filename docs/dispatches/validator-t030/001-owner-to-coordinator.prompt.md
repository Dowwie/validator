# Independent practical oracle after the public candidate freezes

Existing Sol-high coordinator in `/Users/dowwie/MyProjects/validator`.
Save acknowledgment to
`docs/dispatches/validator-t030/001-coordinator-to-owner.response.md`.

## Outcome and conditional start

Produce the T030 independent hard-label oracle from the exact accepted T028 native
sources. This task depends on T028, not T029. Authoring it before preparation
reduces accidental dependence on the conversion implementation.

Start only once both T027 developer outputs are complete, its candidate is frozen,
and its independent read-only review is actively assigned. Until then T027 keeps
the sole developer. During review, assign one fresh Terra-high, fork-none oracle
author with the python-dev skill. No delegation by the author. Preserve the four
active roles and one implementation writer. If T027 needs a correction, let the
oracle author finish its current small coherent checkpoint, save it, and yield the
writer; do not wait for the entire oracle task. Resume from saved output afterward.

T028 acceptance is recorded in
`docs/dispatches/validator-t028/006-owner-to-coordinator.prompt.md`.
Its manifest hash remains
`860300bc288ad1f46c98631f78ad353066d7798a54c182ac5b60d8a1b693444e`
and persisted map hash remains
`a987df05727164c382f0ea404bd4f465aa9dbaf7fb3f6e8c74d5bedac5cd32b4`.
The historical embedded candidate status does not undo the subsequent external
acceptance decision and must not be rewritten.

## Independent inputs and bounded deliverable

Read complete T030 and its linked normative specification sections. Give the fresh
author only the governing specifications, repository/skill rules, accepted source
identity, protected raw/reference/request/map evidence, and this contract. It must
not read `src/`, Validator run outputs, preparation implementation or output,
earlier production numerical fixtures/handoffs, or another author's expected
metrics. Avoid giving it an aggregate acceptance history containing those results.
Historical frozen projections may be inspected only as the specified comparison
evidence; independently compute native results before explaining their difference.

The author owns `scripts/acceptance/oracle_chord.py`, minimal directly relevant
synthetic script tests if needed, and protected
`/Users/dowwie/.local/share/validator/acceptance/chord630/expected.json`.
It saves its complete method, input/output hashes, criterion mapping, exact checks,
and known limitations in its specified dispatch response. Coordinator owns shared
documentation, plan ownership maps, index and Fizzy. No Rust edits or runs, no new
runtime/state engine, no private payloads in public fixtures or logs. Use existing
Python tooling when present; stdlib is adequate for this bounded conversion/count
tool. Apply the python-dev skill without creating an unrelated packaging project.

Compute native Choice and frozen scalar-mapped Score outcomes directly from all
630 saved raw routes, join the 603 labeled references, preserve the 27 exclusions,
and independently calculate both confusion matrices, required hard metrics and
their exact integer/rational operands, cohort counts, paired transitions,
recovered/regressed/changed IDs, and inspectable known failures. Follow the actual
frozen score mapping and tie behavior. Never substitute argmax or silently omit a
recoverable historical malformed projection. Select known failures by a stated
deterministic order after computing them; do not select for a preferred winner.

For consistent preparation and oracle selections, the owner fixes this operational
targeted-pass example before any measurement: take the first 50 labeled source IDs
in ascending exact string order, then map them through the persisted UUID map.
This is a reproducible partial-population demonstration, not a representative
sample or optimized conflict set. Full, representative-cohort, challenge-cohort
and this targeted population remain separately identified. No desired performance
result may change this selection. T029 will use the same rule without importing
the oracle implementation or deriving its transformations from expected metrics.

Run the prescribed `--bundle <frozen-root> --out <expected.json>` interface and
appropriate focused script checks. Ensure deterministic expected bytes, unchanged
protected source/ID-map bytes and secure output permissions. No self-hash cycle.
Preserve exact integer/rational expressions where appropriate rather than using
Validator's floating output as the oracle. Explain historical projection/population/
route differences only after obtaining independent results; do not force agreement
with any headline.

## Review and return

Once the verifier role is free after T027, assign a fresh Sol-high/fork-none
read-only verifier of the frozen oracle script, expected output, raw inputs and
original T030 criteria. It must not use Validator or preparation to establish the
expected result. Reconcile the actual counts, populations, mapping and transitions
with the smallest independent derivation that covers the contract. Do not rebuild
the Rust suite for a source-only oracle. One finding-driven repair and focused
recheck are authorized; repeated incompletion returns to owner with its cause and
a changed bounded approach. No oracle values may be changed merely to match a
later Validator run.

Search/update the General-board card, index all new public and protected governing
artifacts, preserve sensitive payloads, verify rendered notes/first incomplete
step, and run plan/link/diff checks. Return a complete exact-identity handoff and
independent verdict for owner acceptance. Keep caffeinate PID84732 active.
This authorizes T030 only at the stated boundary; T029 and T031+ remain pending
separate dispatches.
