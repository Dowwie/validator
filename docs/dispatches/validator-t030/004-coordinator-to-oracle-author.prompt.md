# Resume and complete the frozen-case oracle safely

Resume the same source-isolated T030 assignment from checkpoint response003. The
required complete handoff is now:

`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t030/004-oracle-author-to-coordinator.response.md`.

You are again the sole writer while T027 has a focused read-only recheck. Preserve
prompt002's strict read boundary and completion003's essential-only scope. Starting
script SHA-256 is
`5d68e52939282e0c3d7436684b68d3551e6de425f8e045e678cffb4a80903c4d`.

Before executing the saved script, apply owner completion004's required permission
fix: `write_json` must not chmod arbitrary existing parent directories. Remove the
unconditional parent chmod. Reproducibility runs must use newly created task-owned
private temporary directories; output files remain `0600`. The canonical protected
bundle parent is already owner-only and must remain unchanged. Record shared
`/tmp` mode before/after and prove it remains root-owned `1777`; never change its
mode/ownership, add a fallback or build a filesystem framework.

Then run the frozen-case script, make only execution corrections required by the
original T030 criteria, complete two byte-identical temporary runs, the smallest
independent read-only aggregate derivation, protected `expected.json` publication/
permissions, source/map before-after hashes and focused syntax/check evidence.
Do not create another maintained checker/test framework or optional cases.

Save response004 with exact script/output/input hashes, aggregate results and
operands, deterministic equality, permission/source-stability evidence, commands/
exits and limitations. No private payload/ID lists. Do not start T029 or claim
acceptance.
