# Acknowledgment of conditional independent T030 oracle authorization

I accept the conditional T030 boundary. T027 remains the sole implementation
priority. I will not start the oracle author until both T027 developer outputs are
complete, the combined candidate is frozen and independent T027 review is active.

At that boundary, one fresh Terra-high/fork-none author will use python-dev and
only the governing specifications, accepted protected T028 raw/request/reference/
map evidence and this contract. It may not read Validator `src/`, run outputs,
preparation implementation/output, prior production numerical fixtures or another
author's expected metrics. It owns only the oracle script, minimal focused tests
if needed, protected `expected.json` and its complete dispatch handoff.

The oracle will independently decode both complete 630-record native routes, use
the frozen score mapping/ties, join 603 labeled references, preserve 27 exclusions
and compute the required matrices, exact operands/metrics, cohorts, transitions,
IDs and deterministic known-failure selection before comparing historical
projections. The fixed targeted example is the first 50 labeled source IDs in
ascending exact string order, then mapped through the persisted UUID map. It is a
partial demonstration, not a representative or optimized population.

If T027 review needs repair, the oracle author saves one small coherent checkpoint
and yields the sole writer. After T027 releases the verifier, a fresh Sol-high
reviewer checks the oracle without Validator or T029. The accepted T028 manifest/
map hashes remain `860300bc288ad1f46c98631f78ad353066d7798a54c182ac5b60d8a1b693444e`
and `a987df05727164c382f0ea404bd4f465aa9dbaf7fb3f6e8c74d5bedac5cd32b4`;
their bytes remain unchanged. T029 and T031+ stay undispatched; caffeinate PID84732
stays active.
