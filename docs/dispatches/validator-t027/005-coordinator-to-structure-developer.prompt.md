# Route the Episode-owned input to real public inspection

Continue prompt002/003's same output and response path. The reported compile
result demonstrates the one missing real consumer inside owner001's authorized
minimal caller wiring. T027 ownership now includes `src/app.rs`; proceed without
another owner gate.

Make the smallest app integration that obtains the selected episode's opaque
input from the already validated single-label or multi-label evaluation row and
clones it only when constructing the owned public `InspectionResult`. Remove the
independent golden-snapshot input re-decode from inspection once the validated
Episode path supplies the same exact value. Preserve replay verification before
disclosure, selected-ID behavior, source resolution, final evidence, evaluation
configuration, privacy and all exact arbitrary-precision/marker/null regressions.

Do not expose model internals, add a generic task trait/registry, clone input into
metrics/comparison rows, or alter inspection schema/output bytes beyond sourcing
the already required `input` value from the checked Episode. Keep any task-specific
lookup method private and minimal. Existing S27/S28, relocation, both-task and
schema tests must prove no behavior change.

Updated routing identities:

| Artifact | SHA-256 |
|---|---|
| `docs/plans/validator/tasks/T027.json` | `82df45c1655fca9abad8d72ea33bc4cb2be64c5a8ffee187d277e6883b5ad01f` |
| `docs/plans/validator/physical-map.json` | `08c68ddd91e5f3ac3a9f231d369bf78383832556f94e8174a6ee16a21f5f9d6d` |
| `docs/plans/validator/tasks/T032.json` | `7e4c6cbfc7325adafd372290eb3397cceea9865addae0146879b2a73c7de886b` |
| `docs/plans/validator/tasks/T033.json` | `1fca1a28670a906457bc4c2052f1b578863d2cbee99d136496d5f0f3b9f326ca` |
| `docs/plans/validator/tasks/T034.json` | `83495835b9905cc3a2aaec233aa3c52dccaff858fe4f986c0a0fec11bacbe64e` |

`verify-plan.rb` and `git diff --check` pass after routing. Compile immediately
after the caller change, then complete every focused behavior check and all output1
gates from prompt002. Record the initial E0282 annotation correction, this exact
Episode consumer, clean diagnostic result and all hashes in response002.
