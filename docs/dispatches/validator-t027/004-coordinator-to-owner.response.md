# Acknowledgment of T027 structural evidence method

I will carry this clarification into output2 and the independent review without
changing output1's six-file integration scope.

Each nonzero `structure_dm01` through `structure_dm12` filter must execute the
assigned real public behavior with discriminating assertions. Where a row also
asserts source structure, concrete checked types, signatures, caller ownership or
shared implementation paths, the handoff and verifier must add explicit direct
source/signature/ownership review. The behavioral filter and static review are
one mapped proof; neither substitutes for the other.

DM01 will therefore combine both real task workflows with review of their shared
backbone. DM02 will combine real cross-kind target/probability/policy rejection
with review of concrete evaluator signatures and callers. Other DM rows will use
the same property-specific split where needed.

No compiler-test framework, source-text-matching test, private-model export or
duplicate scenario is authorized merely to force static properties into runtime
tests. Exact DM names, all public assertions, clean gates and the single combined
independent review remain required. This adds no task or review layer.
