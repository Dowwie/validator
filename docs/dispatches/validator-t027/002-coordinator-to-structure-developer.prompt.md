# Integrate real checked structure and reach clean existing-suite gates

Role/model: fresh sole developer, `gpt-5.6-terra`, high reasoning,
`fork_turns: none`. Do not delegate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #190 — Verify Validator module boundaries and offline suite](http://localhost:3006/1/cards/190).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t027/002-structure-developer-to-coordinator.response.md`.

Save the full substantive handoff before returning only path, SHA-256 and status.
You are the sole code writer. Do not edit governance, plans, index, acceptance/
session records or Fizzy. Do not touch the protected T028 bundle or Chord. Do not
start output2, T029 or later work. Do not delegate.

Read `/Users/dowwie/.codex/AGENTS.md`, repository `AGENTS.md`, complete T027,
owner prompt001, the main specification's project-organization/dependency/test
sections, shared data-model structures/ownership/DM checks, execution contract
and manage-dev-team skill. Preserve accepted T026 manifest017/Ready018 behavior.

## Mandatory pre-edit routing report

First inspect the actual source flows and run the exact warning-denied Clippy
command. Before changing any repository file, send the coordinator a concrete
plan mapping every staged diagnostic to:

- the required real construction/ownership flow and exact file/signature changes;
- a genuinely obsolete build-created helper removal, with why it is not required;
- or a real existing test-local helper move, with why production does not own it.

Name every file you need to write, including minimal caller/signature wiring in
app/evaluation/comparison/artifacts. Do not edit until the coordinator confirms
that task/physical/downstream ownership has been synchronized. This is routing,
not a new scope approval; after the numbered synchronization instruction, proceed
through the complete output without another pause unless a material contract
choice appears.

## Required implementation boundary

Make the smallest genuine structural integration that eliminates the accepted
17 production plus three duplicate lib-test diagnostics:

- required TaskDefinition/task wrappers/Episode and checked task-specific types
  must perform their actual dataset/evaluation/row construction roles;
- opaque input is owned once and borrowed for scoring, never cloned into metric or
  comparison rows;
- task dispatch and outputs, policies, probability vectors and results remain
  closed and task-specific; no Task trait, registry or extension framework;
- strict private DTOs retain tags, schema/version checks, recursive duplicate-key
  rejection, unknown-field rejection, exact opaque JSON/source/observation/
  preparation validation and required validation guards;
- validation/evaluation/comparison remain pure; app owns orchestration and
  recomputation; artifacts owns filesystem/hash/publication;
- keep the thin documented lib/main/CLI API, private construction, module import
  directions, `deny(missing_docs)`, existing complexity limit and current Cargo/
  toolchain/dependency features.

Do not silence warnings with token construction, discard calls, fake uses,
underscore renames, `allow`, test gating of required production types, public
exports, fallback, unimplemented paths or weakened tests/schemas. Python/T028 is
not a Rust consumer. Remove only truly obsolete non-required helpers created by
this build, or move genuinely test-only convenience code into tests. Explain each
disposition against real callers and requirements.

The initially authorized read/write candidates are `src/model.rs`,
`src/model/common.rs`, both task model modules, `src/validation.rs`,
`src/validation/wire.rs`, `src/lib.rs` and existing test targets. Minimal callers
in app/evaluation/comparison/artifacts are allowed only after they appear in the
pre-edit plan and ownership routing. Do not change numerical results, tolerance,
publication, CLI/report behavior, schemas/fixtures or Cargo unless a concrete
current-contract failure is reproduced and returned to the coordinator first.

Compile at the first coherent change. Preserve all 44 library, 11 CLI, 91
conformance and zero doc tests. Finish output1 with these commands serially:

```text
cargo fmt --all -- --check
cargo clippy --all-targets --all-features --locked -- -D warnings
cargo test --all-features --locked
cargo build --release --locked --bin validator
git diff --check
ruby docs/plans/validator/verify-plan.rb
```

Every command must exit 0; no diagnostic allowance remains. Do not add DM01-DM12
or `offline_cli_contract` here unless a tiny assertion is inseparable from the
structural change; output2 owns their complete exact proof.

Starting accepted identities include manifest017
`bb0ea141de203596fcd9ee3c7db8b7bf37f8a2033e7858d05074b9bf03e3cf44`,
T027 `d8707394806408e0678fb4a9c760d1bda250473260ebd2f4685465282afe9979`,
physical map `98dab85533b199862bebbb00894eb81f1216d9e8a255b52dc715cdab613168bf`
and coverage `efe2f425fc975f953c5765d1ea40ed47ffea8f672249d85f4e3eb07c9843e896`.

Response002 must record the pre-edit diagnostic/flow plan, final per-diagnostic
disposition, exact changed/unchanged hashes, module/signature review, behavior
preservation, commands/exits/counts and any unresolved limit. Return only when
the warning-free existing-suite boundary is complete or a concrete material
contract decision is frozen for owner reassessment.
