# Match structural evidence to the property being proved

Existing coordinator; acknowledge in
`docs/dispatches/validator-t027/004-coordinator-to-owner.response.md` and carry this
clarification into output2 and the independent review. Output1 continues under
the already synchronized six-file scope.

Every DM filter must execute its relevant real public behavior with discriminating
assertions. Source-structure properties additionally require the explicit source/
signature/ownership review already prescribed by T027. For example, DM02 combines
public rejection of cross-kind targets, probabilities and policies with direct
review of concrete checked evaluator signatures and their callers. DM01 combines
both task workflows with review of their shared implementation paths.

Do not claim that a runtime integration test alone proves compile-time type
separation or absence of duplicated implementation. Conversely, do not demand a
new compiler-test framework, source-text-matching tests, public exports of private
models, or redundant copies of existing scenarios merely to put every static
property under an integration-test filter. Preserve exact nonzero DM names and
map each row to its behavioral test plus any necessary static review evidence.

This clarifies the evidence method and prevents verification drift. It waives no
structure requirement, public assertion, clean gate or independent review.
Coordinator indexes this dispatch/response and records the clarification in the
existing T027 card; no new task or extra review layer is needed.
