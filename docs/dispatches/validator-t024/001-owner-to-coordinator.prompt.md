# Accept policies/comparison and complete the nine-schema contract audit

Existing coordinator: `gpt-5.6-sol`, high reasoning. Repository:
`/Users/dowwie/MyProjects/validator`. Follow manage-dev-team, the project authority
index and file-based dispatch rules. No implementation/test/verification-code
authorship. Save full acknowledgment to
`docs/dispatches/validator-t024/001-coordinator-to-owner.response.md`.

## Acceptance and next outcome

The owner read complete verdict009, focused verdict014, superseding Ready017 and
coordinator handoff018 in `docs/dispatches/validator-t021-t023/`. Accept the exact
repaired candidate:

- manifest013 `2cd39b7c27d9b0408ff0903441f3afa975616edf3644f3e502f560d6d26f0bc7`;
- Ready017 `0f44bdf19bc2834e563b402b60f2f9669110e452801b7f0da55620d0491b4dcc`;
- handoff018 `1636774148047212a4f4bf28b9d4d55dbcec24c5807f15e98d9289bcf3a78c3b`.

T021-T023 now provide both explicit policies, typed comparison for both task
kinds, and validated intersection recomputation including equal/empty selections.
All 74 tests, formatting and release build pass. Preserve the exact evidence-
placement decision015 and earlier verdicts honestly. Unmodified Clippy still has
17 staged production diagnostics plus three duplicate lib-test diagnostics;
T027/T035 must become fully warning-free. This is not full-product acceptance.
Record acceptance in existing acceptance/session artifacts and complete Card185.

Authorize **T024 only** next. Its outcome is nine complete offline machine schemas
and the exact schema/evidence conformance cases assigned to it by the ratified
plan. Search Fizzy before creating the next relevant General-board card, link the
accepted Card185 dependency and keep all actionable state in that card. Do not
begin T025 or later work before owner acceptance of this checkpoint.

## Roles and bounded execution

Read T024 completely, its linked normative sections and its coverage assignments.
Resolve its historical `implementation_role: independent verifier` metadata to the
governing sole-writer team split: one fresh Terra-high/fork-none developer authors
necessary schemas/tests; one fresh independent Sol-high/fork-none verifier judges
the frozen result. The verifier remains read-only and must derive required fields
and legal alternatives from the specification, not assume emitted documents or
the developer's summary define correctness. Update task/execution role mappings to
make this explicit without changing product criteria. Coordinator owns governance,
index, acceptance-record assembly and Fizzy.

Keep dispatches concise and immediate. Derive the normative schema requirements
before changing the current accepted files. Reuse existing strict definitions and
tests; repair proven gaps, not a wholesale cosmetic rewrite. If all nine schemas
plus their assigned cases exceed one coherent handoff, split local construction
into schema contracts and assigned case evidence, one writer at a time, with a
fresh context for a substantial next output. Advance locally without another owner
gate after a complete handoff. One final independent T024 review follows.

## Scope and concrete requirements

Owned artifacts are T024's nine `schemas/v2/*.schema.json` files and existing
conformance/CLI tests. Use exact artifact paths in hash tables. Preserve accepted
Rust production, fixtures, policies/comparison semantics and unrelated work.
An actual production-output/specification mismatch is a required correction, not
permission to change the schema to match an invalid output: save the precise
reproduction and proposed owning change for the owner before expanding production
edits. Ordinary fixes inside the assigned schema/test scope proceed directly.

The audit must establish all nine Draft 2020-12 schemas load and resolve references
offline with pinned tooling. Required shapes come from the specification and
data-model contract. Check:

- Required root/nested fields, task/status/tag/type alternatives, declared
  cardinalities, UUID/count/digest forms, ratios and explicit metric populations.
- Exact finite/null/undefined/infinite forms, including special-value requirements;
  selected/answered population units; typed abstention columns; both task-specific
  report and comparison families, policies and signal evidence.
- Source definitions/bindings, observation definitions and values, preparation
  descriptors, and their task-specific legal combinations. Opaque input and
  designated configuration/value positions retain their allowed arbitrary JSON;
  their containing records and any required object shape stay constrained. No
  additionalProperties escape hatch outside genuinely permitted opaque positions.
- All actual successful input/output operations and legal status variants, plus
  stable error/receipt/check/inspection documents. Systematically deleting required
  fields and mutating task/status/type must reject. Use discriminating positive
  cases so blanket rejection cannot appear correct.
- Preserve existing strict single/multi family discrimination, zero-exclusion
  intersection, exact opaque large-integer/1e400/null/literal-number-key behavior,
  and all earlier schema regression assertions. Do not weaken tolerances, oracles
  or valid-input support to simplify schema validation.

Complete **every exact verification ID in T024**, including
`all_schema_contracts`, `cli_stdout_schema_matrix` and all assigned `case_s*`,
`case_m*`, and `case_e*` filters. Read their actual normative rows; do not count the
umbrella tests as substitutes. Each must list and execute nonzero on its designated
target and prove that specific case through the real public API or binary where
required. Reuse focused helpers/fixtures and existing assertions without duplicating
decision logic. The evidence cases use public synthetic inputs, never private Chord
files or inference. Do not create a generic audit engine or new framework.

## Evidence, review and return

Map each schema definition and assigned case to its normative clause, valid
document, discriminating mutations, exact commands/results and current hashes.
Use independent expected contract shapes; production output is a subject of
validation, not its authority. Do not require the same assertion in multiple
automated layers when the planned division and independent evidence already prove
the behavior. Record evidence placement accurately.

Run the task's exact filters with nonzero listing confirmation, then final pinned
fmt, unmodified warning-denied Clippy, full locked tests, locked release build and
diff checks. The exact 17+3 staged inventory may remain only until T027; no ordinary
or new warning class, suppression, fake consumers or widened exports is allowed.
No unrelated dead-code cleanup now. Preserve one writer and serialize Cargo checks.

Freeze exact candidate hashes and dispatch the fresh Sol-high independent verifier
for **T024's current contract only**. Review the scoped schemas/tests and necessary
surrounding interfaces against the specification; independently execute relevant
positive/negative evidence, reuse unchanged gates and stop when a verdict is
supported. Findings need a current requirement, location, reproduction, consequence
and smallest correction. No numerical reimplementation, open-ended fuzzing, optional
style request, provider/private-data work or future T025-T035 audit in this review.

One coordinator-managed finding-driven repair/recheck cycle is authorized. A
repeated incomplete handoff, a material scope/contract gap or unresolved correction
returns to the owner with actual evidence and a changed bounded approach. Do not
restart the same unfinished bundle or add approval pauses for routine owned fixes.

Index all created dispatch/response/artifact paths, link them in Fizzy and verify
rendered content/first incomplete step; update required task/physical mappings,
plan/index links and diff checks. Preserve the unborn/untracked workspace, pinned
dependencies, unrelated files and current caffeinate hold. No staging, commit,
reset, deployment, source-data mutation or broad cleanup. Return one complete saved
verdict and coordinator handoff for owner acceptance before T025.
