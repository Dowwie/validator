# Focused recheck of repaired T024 schema completeness

Role/model/context remain the same independent verifier from verdict006,
`gpt-5.6-sol`, high reasoning. Do not delegate. Keep the repaired candidate
read-only.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #186 — Audit Validator's nine machine schemas](http://localhost:3006/1/cards/186).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t024/009-verifier-to-coordinator.response.md`.

Save the full substantive Ready/Revise/Blocked focused verdict there before
returning. In chat return only the path, SHA-256 and verdict. Do not edit any
candidate/governance/tracking file. Do not delegate.

Reconcile repaired manifest008 SHA-256
`91d756bbb4253a832461c2e3642a585c00286caf471b3776abf5af8325b5e0d3`,
including its inherited manifest005 hashes and four repaired file hashes. Read
complete repair response007 and original verdict006. Stop Blocked on mismatch.

Recheck only the single reproduced finding and justified affected neighbors:

- comparison side source definitions/counts, single/multi counts, class/macro/
  transition/probability/episode-set fields are closed, typed and accept current
  legal single/multi documents;
- scalar metric population scope excludes `raw_answered`, while explicit bin
  population definitions still admit it where normative;
- report source/preparation configuration is an object with arbitrary member
  values (including null, large integer, `1e400`, literal tag-shaped object),
  golden input stays arbitrary, and stored artifact paths accept legal snapshot/
  evidence forms while rejecting absolute/traversal forms;
- receipt paths remain absolute and operation-specific (`report.json` versus
  `comparison.json`);
- every verdict006 mutant rejects one at a time beside its named legal neighbor;
  zero-exclusion intersection, task discrimination, both umbrellas and all
  fourteen assigned rows remain preserved by exact hashes and affected evidence.

Use the existing isolated target/build evidence from verdict006 or the already
built repository target. Do **not** create another fresh target or repeat the full
suite/release merely for confidence. Run only the focused commands needed to judge
the repaired delta, at minimum:

```text
cargo test --locked --test conformance all_schema_contracts -- --nocapture
cargo test --locked --test cli cli_stdout_schema_matrix -- --nocapture
```

Reuse the repair developer's passing 36 conformance, 10 CLI, 44+36+10 full suite,
fmt, release, diff and exact 17+3 Clippy evidence unless a focused failure requires
more. You may adapt the existing external pinned mutation probe to the repaired
hashes, but do not expand it beyond verdict006 admissions and legal neighbors.

Return **Ready** if the exact repaired candidate resolves the sole finding and
preserves affected neighbors. Return **Revise** only for a concrete remaining or
repair-caused current T024 defect with reproduction and smallest correction.
Return **Blocked** only if reconciliation/evidence prevents judgment. Do not reopen
the full schema audit, request optional metadata/framework work, duplicate settled
assertions or inspect T025+. Stop once the focused evidence supports the verdict.
