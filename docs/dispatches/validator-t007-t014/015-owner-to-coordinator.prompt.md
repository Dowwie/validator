# Complete the missing T010 regression matrix

Role/model: existing Sol-high coordinator, retained context.
Required response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t007-t014/015-coordinator-to-owner.response.md`.

The owner read corrected response013 and escalation014. The schemas changed,
but the required regression assertions did not. The cause is an incomplete
evidence deliverable being reported as a completed correction; one passing filter
does not establish cases absent from its test body. Do not accept that handoff.

Authorize one bounded completion attempt with the same sole developer. Its
primary write is `tests/conformance.rs`: implement every missing positive/negative
case enumerated in escalation014 and correction013, using named cases whose
expected accept/reject result comes from the exact spec. Include legal multi-label
prediction and threshold configuration and a named shape-valid unknown-source
case demonstrating the schema/runtime boundary. Do not route schema-only cases
through an invented application API or generate expectations from the schemas.

Preserve the corrected schema files. If one required regression exposes a real
remaining schema violation, permit only its minimal correction in the existing
three schemas, record the failing/passing case, and preserve every other criterion.
No dependency update, new schema architecture, unrelated production change,
oracle alteration, or extra test framework is authorized.

Run the exact schema filter, format, full library regression and diff check.
The saved complete response must map each requested case family to its actual
case name/source location and result, with changed hashes. The coordinator must
reconcile those actual assertions and the command results before advancing;
prose saying coverage exists is insufficient. Reuse unchanged source evidence.

The next attempt changes the deliverable to the concrete missing test matrix,
rather than repeating schema design instructions. Its value is material because
these published schemas define the machine contract used by later agents.
On a complete local handoff advance automatically to T011 under the existing
atomic schedule; do not add an independent review now. Another incomplete or
unsubstantiated handoff returns directly to the owner, without reassignment loops.
The eventual combined review allowance remains unchanged.

Save/index/link the dispatch and acknowledgment, restore active Fizzy state,
preserve caffeinate, and continue the authorized build.
