# Complete the remaining T010 evidence with a fresh developer

Role/model: existing Sol-high coordinator, retained context.
Required full response: `/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t007-t014/021-coordinator-to-owner.response.md`.

The owner has read escalation020 and the existing owner018 contract. Repeated
handoffs have claimed completion without adding the listed discriminating
assertions. Preserve the corrected schemas and passing tests, but retire the
current scorer_developer from implementation. Dispatch a fresh sole developer
using gpt-5.6-terra, high reasoning, fork_turns none. This is a staffing correction,
not another verification gate or a reset of the combined review allowance.

Save the fresh developer's complete instructions and response paths as numbered
files. Include the global `/Users/dowwie/.codex/AGENTS.md`, repository AGENTS.md,
the relevant specifications, T010 task, and owner018/this decision. The exact
remaining task is the following test matrix in `tests/conformance.rs`:

1. Separate the bundled invalid Bernoulli value and envelope/row/outcome extra
   fields into isolated mutations of a valid prediction document.
2. Add an isolated whitespace model case. Retain the existing isolated source-key
   regression and inspect shared nonblank-reference field wiring; do not expand
   into exhaustive cross-products.
3. Separate empty thresholds from an extra policy field using a valid config.
4. Cover legal categorical probabilities and empty label-marginal probabilities,
   retaining the existing legal label-marginal and empty categorical cases.
5. Cover wrong scalar/vector observation fields, a blank vector key, and empty
   label-marginal observation values, retaining the empty categorical case.

Use valid bases with one invalid mutation per negative. Preserve existing valid
assertions and legal scalar values outside [0,1], unnormalized observation vectors,
all five legal observation kinds, policies and multi-label shape cases. The
existing absent-source case suffices for the runtime-only boundary; no second
example is required. No dependency, framework, production-feature or schema
redesign. Change an existing schema only if a listed isolated case exposes a
concrete specification defect, reporting that defect explicitly.

Completion requires actual case-name/location/result evidence, exact changed
hashes, `cargo fmt --check`, the `input_schema_contract` conformance filter,
library regression tests, and `git diff --check`. Coordinator inspect the actual
new assertions once against these five bullets; do not accept prose or test-count
claims alone. On complete evidence, proceed automatically to T011 under the
existing atomic-task and combined T007–T014 review contract. On another incomplete
return, report the precise missing assertion and cause before further attempts.

Fresh developer contexts may be used for subsequent atomic tasks if retaining
context again yields incomplete handoffs; keep one implementation writer, the
prescribed role/model settings, and file-based dispatches. Do not insert extra
independent reviews. Restore Card183 to Working On with accurate next action,
index and link this prompt and all responses, and preserve the task caffeinate
PID84732. The owner reconfirmed its active sleep assertions at this checkpoint.
