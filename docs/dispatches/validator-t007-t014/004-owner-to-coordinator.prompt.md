# Use atomic implementation handoffs within the combined acceptance boundary

Role/model: existing Sol-high coordinator, retained context.
Required response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t007-t014/004-coordinator-to-owner.response.md`.

The owner read developer001, continuation003, its incomplete response, and
escalation004. The new worker produced a passing T007 slice, then returned twice
before implementing any of the remaining much larger eight-task sequence. This
shows that implementation-turn granularity needs to match the plan's atomic task
units. It does not justify another staffing change or repeated instructions to
finish all eight tasks in one response. This is the owner's reassessment of that
repeated incomplete handoff: keep the current worker, narrow its next complete
deliverable to T008, and preserve the existing verified baseline. Completing the
scorer materially advances the required CLI path; one atomic assignment is the
smallest changed attempt that tests whether the scheduling correction resolves
the premature-return behavior.

Keep **one combined T007–T014 independent review and owner acceptance**, preserving
the real application API construction dependency and all final gates. Change the
developer handoff cadence to sequential, bounded implementation units:

- Retain the existing T007 local result; any still-missing T007 contract behavior
  must be completed as its consumers require, with local tests and explicit evidence.
- The current next handoff completes **T008** and its real owning-module tests,
  independent fixture oracle, and coherent compile/format checks. Conformance
  cases needing the T014 app API remain explicitly pending; no private API is
  exported or duplicate implementation created to run them prematurely.
- Then dispatch **T009**, **T010**, **T011**, **T012**, **T013**, and **T014** in
  dependency order as individual implementation units, each with its exact task
  contract, allowed artifacts, required local evidence, and saved result. Advance
  automatically from a complete local implementation handoff to the next unit;
  no owner approval or independent verifier turn is needed between these units.
- After T014, reconcile every constituent criterion, run all final named API/CLI
  filters and clean pinned gates, freeze once, and perform the already-authorized
  combined independent review. No task is called independently accepted earlier.

Within each unit the sole developer continues until its bounded implementation
and available local verification are complete. A legitimate completed unit is a
milestone, not an incomplete whole-group handoff. An actually incomplete unit
without a blocker still triggers the existing reassessment rule; do not conceal
missing required work by redefining what its contract contains. Preserve current
edits and read-only review evidence; do not restart passing T007 work.

This reconciles the task decomposition protocol's atomic implementation units
with the API-dependent combined acceptance gate. It changes scheduling only:
all requirements, tests, private boundaries, dependencies, source baseline,
one-writer rule, and the combined review's one-repair allowance remain unchanged.
Do not create extra reviews, add tasks, or rerun unchanged full suites at every
unit by habit. Use relevant local checks and the required final integration gate.

Save the narrowed active developer instruction before delivering it, and preserve
earlier prompts/responses. Update the execution contract/build charter with this
handoff rule and keep actual unit/state in Fizzy. Index/link prompt/response and
worker handoffs, verify plan/index references, retain caffeinate, and continue
without waiting for the user or owner between completed implementation units.
