# Reassign the unchanged atomic scorer after repeated premature returns

Role/model: existing Sol-high coordinator, retained context.
Required response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t007-t014/007-coordinator-to-owner.response.md`.

The owner read developer005 and escalation006. Narrowing the assignment did not
resolve this retained worker's premature returns. T008 itself remains concrete,
bounded and feasible; no technical blocker or design conflict has been reported.
The missing typed metrics, evidence and tests remain necessary for the authorized
check/evaluate outcome.

Retire `/root/coordinator/replacement_developer` from further work. Provision one
fresh **gpt-5.6-terra, high, fork_turns=none** developer as the sole writer, within
the four-role concurrency limit. Keep the same coordinator and independent
verifier. The former developer receives no further assignments or writes.

The new assignment is **complete T008 only** from escalation006's reconciled
partial source. Keep the useful accessors/matrix counter if sound; do not rebuild
accepted admission or repeat T007's work. The changed approach is a fresh context
containing one atomic outcome, rather than continuing the retained context that
has repeatedly returned incomplete. A fresh context previously completed the
admission repairs; this is a bounded staffing correction, not a scope change.

Make the file-based brief concise and self-contained: governing global/project
AGENTS and required skills, exact T008 task/spec sections and F04/thread oracles,
current source/metric interfaces and hashes, allowed paths, complete local tests,
and saved response path. The worker need not read the full dispatch history or
all eight future task contracts. The two application-API conformance filters
remain explicitly pending for T014; owning tests must prove the complete scorer
now. Required minimal T007 metric helper completion within its owning files is
allowed if an actual T008 contract needs it; record it and preserve existing tests.

Require a compiling complete hard-metric path and an actual local T008 test as
the next progress evidence. Check after approximately ten minutes; on an incomplete
return without a concrete blocker, stop and return the exact state to the owner.
Do not reissue the same instruction or count a source sketch as task completion.
Ordinary debugging inside this bounded implementation is authorized.

On a complete locally verified T008 handoff, proceed automatically to the next
atomic unit under owner004, retaining the one final T007–T014 review and owner
gate. No independent review or repair allowance has been consumed or reset by
these incomplete implementation turns. No extra writer, feature, audit or
publication is authorized.

Reconcile hashes before the replacement edits, restore the active Fizzy state,
record retired/replacement handles, index/link dispatch/response artifacts, and
preserve caffeinate PID84732/session29011. Save this acknowledgment and continue.
