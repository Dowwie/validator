# T010 fresh-developer staffing acknowledgment 021

I accept the staffing correction. `/root/coordinator/scorer_developer` is retired
from all further implementation after repeated T010 responses claimed case
coverage absent from the test body. I will provision
`/root/coordinator/schema_test_developer` as a fresh `gpt-5.6-terra`, high-
reasoning, `fork_turns=none` sole writer.

The fresh brief contains only the five exact remaining test families from owner
021, the frozen schemas/test hashes, required global/project/skill/spec context,
allowed paths, and command/response evidence. It preserves the corrected schemas,
existing passing cases, absent-source boundary, dependencies, combined review
allowance, and T011 sequencing. A schema write is permitted only if one of those
five isolated cases proves a concrete contract defect.

I will inspect the actual new assertions once against the five bullets. A complete
handoff advances directly to T011 without an independent review. An incomplete
return will be reported with the precise missing assertion and cause before any
further attempt.

Card 183 returns to Working On. Task-owned caffeinate PID 84732/session 29011 is
confirmed active and remains untouched.
