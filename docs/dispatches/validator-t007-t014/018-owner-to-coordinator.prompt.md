# Finish discriminating schema cases without expanding the evidence gate

Role/model: existing Sol-high coordinator, retained context.
Required response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t007-t014/018-coordinator-to-owner.response.md`.

The owner read escalation017, response016, the actual 185-line conformance test,
and predictions.schema.json. The escalation mixes required corrections with one
unsupported additional demand. Apply this bounded reassessment.

## Required cause and correction

Several negative documents contain multiple independent invalid fields. For
example, the bounded-observation case also adds top-level, row and outcome extras;
the whitespace-source case also has a blank model. A false schema result cannot
establish which promised rule rejected those documents. There are also missing
positive observation and reject-below variants. The next approach is valid named
bases with one invalid mutation per negative case, not larger bundled invalid
documents or more prose claiming coverage.

There is a concrete remaining implementation defect: the top-level `sources`
schema has `additionalProperties` but no `propertyNames` constraint, so a source
key consisting only of whitespace with an otherwise valid model/configuration
is accepted. The confounded source/model test conceals it. Add its isolated
regression and the minimal existing-schema constraint; preserve all other schema
contracts and dependencies.

Authorize the same sole developer to complete only:

- Separate the existing bundled extra-field, bounded-value, whitespace and empty-
  threshold negatives so each establishes its named rule from a valid base.
- Add a prediction `label_set` rejection, a valid complete reject_below config,
  and a legal observation fixture containing all five kinds. Add discriminating
  bounded-scalar, vector-empty/wrong-field and vector-key negatives for the
  separately defined observation shapes. Retain scalar values outside [0,1] as
  legal and retained categorical sum 0.99 as legal.
- Cover the previously requested nonblank/map rules with isolated mutations;
  reusable mutation tables and shared valid bases are appropriate. Inspect the
  shared nonblank definition's wiring instead of inventing cross-products of
  every tag and identical reference. Both probability variants have separate
  schema branches, so cover their legal/empty-map shapes without redesigning them.
- Retain the existing golden task/target, source-profile, policy, explicit-null,
  version/type/unknown-field and legal multi-label evidence. Do not delete a valid
  assertion when restructuring the confounded cases.

Primary write remains tests/conformance.rs, with minimal changes in the existing
three schemas only when a required case demonstrates a contract defect. No new
dependency, framework, production feature or schema redesign. Run the same
schema filter, format, library regression and diff checks. Save exact case-family
to-name/location/result evidence and changed hashes; reconcile actual assertions.

## Scope stop

The existing named absent-source case satisfies the requirement for **at least
one** shape-valid runtime-only boundary. A second boundary was never required by
owner015 or T010; remove it as a blocking demand. Do not require exhaustive input
combinations or a separate assertion for each identical shared rule reference
when the rule and its field wiring already have sufficient evidence.

The required result is a small discriminating matrix for the specified shapes,
not a new schema audit programme. Once the listed gaps and concrete source-key
defect are closed with actual evidence, advance to T011. No independent review is
added here and the combined T007–T014 review allowance is unchanged. Another
incomplete return or concrete unresolved defect comes to the owner; do not add
new optional coverage conditions or silently repeat this assignment.

Record the required/optional distinction in the saved acknowledgment and Fizzy,
index/link instructions/results, preserve caffeinate and continue active work.
