# Recheck the repaired T026 exact-filter proofs

Role/model: same independent verifier from verdict010, `gpt-5.6-sol`, high
reasoning, original `fork_turns: none` context. Do not delegate. You did not
implement this candidate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #188 — Verify Validator artifact integrity](http://localhost:3006/1/cards/188).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t026/013-verifier-to-coordinator.response.md`.

Save the full substantive Ready/Revise/Blocked verdict before returning only its
path, SHA-256 and verdict. Do not edit repository files or Fizzy. Temporary
read-only probes may live outside the repository. Do not delegate.

Reconcile repaired manifest012 SHA-256
`4fc0552b87d895a5597977a12884fb09b6c1e5c2145152ede8551b54abb85895` and every listed
hash before review. Stop Blocked on any mismatch. Read complete verdict010,
repair prompt011 and response011. The original broad review is settled: recheck
only the six corrected proof gaps and justified affected regressions. T027/T028
and optional hardening are outside scope.

Confirm:

1. S15 proves both fixed origins and complete Q1/Q2 source definitions on both
   sides without copying expected values from current output.
2. S24 proves relocated public comparison and actual comparison publication with
   every original input absent.
3. S26 proves the real evaluation receipt's absolute report path and independent
   exact-byte hash while retaining comparison-receipt evidence.
4. S27 uses a real public typed failure and serialized machine error to prove
   sentinel privacy, alongside the settled success/report checks.
5. M17 proves exact vocabulary order `[A,B]` before transition cells.
6. M18 independently derives the expected conditional values, delta,
   populations/availability, and absence of winner/improvement representation.

Run the six affected filters and the two matrices/accepted replay regression.
Reconcile inventory 91 conformance/11 CLI. Reuse the developer's unchanged full
suite, format, release and diff evidence. Run warning-denied Clippy only if source
identity or inspected edits make the staged-diagnostic conclusion uncertain; do
not rerun the entire settled review by habit.

Return **Ready** if the exact repaired manifest and all six proof corrections
satisfy the original T026 contract with no known blocking defect. Return
**Revise** only for a reproduced current-contract defect, with exact evidence and
smallest correction. Return **Blocked** only when identity/evidence prevents a
judgment. Stop once this bounded recheck supports a verdict.
