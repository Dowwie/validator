# Complete the original T026 output-1 replay matrix

Resume as the current sole Terra-high developer. Do not delegate. Preserve all
useful output1 work and exact named filters. Save the new complete handoff to:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t026/005-safety-developer-to-coordinator.response.md`.

Read owner prompt004 before editing. Response002 and response003 remain historical
partial handoffs; do not overwrite them. This is bounded completion of prompt002,
not a new repair allowance. Production and allowed ownership boundaries remain
unchanged.

The current shared replay scenario is insufficient because it constructs only the
single-label `replay_run()` and compares `inspect_replay(...).is_ok()` to a boolean.
Complete `artifact_adversarial_matrix` while preserving the accepted
`replay_binding_and_result_tampering` wrapper and the 81-test inventory.

## Required completion

Use actual single-label and actual multi-label saved-run fixtures with appropriate
evidence. Reuse current setup/mutation helpers and introduce only small explicit
task-specific choices. Do not create a generic framework, dynamic metric-field
discovery or copied production logic.

For the valid unmodified control and every required invalid mutation:

- call verified inspect through the real public path and assert the exact
  spec-derived error code/category/stage, not only `is_err`/`is_ok`;
- call comparison with an isolated nonexisting destination, assert the exact
  public error category, and prove the comparison destination remains absent;
- make each condition discriminate its intended contract: golden/predictions/
  config snapshot bytes or digest; stored result; missing/extra/duplicate/swapped
  source-index binding; wrong original string; wrong ordinal stored path; wrong
  evidence index; changed evidence byte; escaping stored symlink; report source-
  array disagreement; count/status/recorded-configuration mutation.

For each task kind independently:

- mutate one complete computed metric value at a complete fixed field path by an
  amount inside the declared replay tolerance and prove verified inspect and
  comparison succeed;
- mutate an exact count and prove typed failure through inspect and comparison
  with no published comparison;
- mutate recorded configuration or a metric-shaped configuration member and prove
  exact typed failure; tolerance must never apply to recorded data;
- mutate status where applicable and prove exact rejection;
- use an actual multi-label computed path for the multi-label run, not a
  single-label key or arbitrary tail-name match.

Keep the valid unmodified control for both tasks. Identify exact field paths and
expected error categories in the test and handoff. If a case exposes a production
violation, freeze its input/expected/actual evidence before making only the
already-authorized smallest `src/artifacts.rs`/`src/app.rs` correction. Stop for
any other owning-layer or contract change.

Then reconcile the remaining prompt002 requirements against actual assertions:
relocation/copied evidence, UTF-8/index/repeated-path ordering, deterministic
publication, real CLI exits, privacy, exact receipts and large-integer inspection.
Reuse passing S23-S28 and CLI matrix evidence where unchanged; a named wrapper is
not behavioral proof.

## Completion boundary

Run both shared replay names, both matrices, every exact S23-S28 filter, list and
full affected targets:

```text
cargo test --locked --test conformance replay_binding_and_result_tampering -- --nocapture
cargo test --locked --test conformance artifact_adversarial_matrix -- --nocapture
cargo test --locked --test cli publication_and_privacy_matrix -- --nocapture
cargo test --locked --test conformance case_s23 -- --nocapture
cargo test --locked --test conformance case_s24 -- --nocapture
cargo test --locked --test conformance case_s25 -- --nocapture
cargo test --locked --test conformance case_s26 -- --nocapture
cargo test --locked --test conformance case_s27 -- --nocapture
cargo test --locked --test conformance case_s28 -- --nocapture
cargo test --locked --test conformance -- --list
cargo test --locked --test cli -- --list
cargo test --locked --test conformance
cargo test --locked --test cli
cargo fmt --all -- --check
git diff --check
```

The expected inventories remain 81 conformance and 11 CLI unless a necessary new
exact test changes and explains them. Do not run output2/final gates. Response005
must identify both task fixtures, each mutation/error/publication outcome, fixed
computed field paths and tolerance/exactness evidence, complete remaining output1
mapping, commands/exits/counts and exact hashes. Return only with a genuinely
complete output1 or a frozen concrete product/scope discrepancy.
