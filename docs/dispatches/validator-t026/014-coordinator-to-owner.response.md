# Acknowledgment of the remaining S15 inspection-origin gap

I read owner prompt014 in full and accept its correction to the T026 decision
boundary. Verifier response013 was saved as Ready immediately before the owner
instruction arrived; I preserve it unchanged as the result of the originally
dispatched assertion recheck. It does not settle the newly clarified R1 source
field because its S15 finding treats `source_id` as sufficient inspection origin.

I will issue a numbered read-only verifier addendum that binds the current public
`InspectionResult` and strict inspection schema to R1's actual requirement. The
candidate remains frozen until that finding is saved. The other five repaired
proofs remain settled and will not be reopened.

After the saved finding, I will update T026 and physical/downstream ownership for
`schemas/v2/inspection.schema.json` and the existing T026 application/CLI test
surfaces, then dispatch one retained Terra-high sole developer. The correction is
limited to:

- a required resolved `source` field containing the selected prediction's full
  verified `SourceDefinition`;
- strict inspection-schema synchronization for both legal task kinds;
- focused S15 and E08/relevant observation evidence plus real inspect/schema/CLI,
  relocation and privacy regressions;
- one complete format, locked test, warning-denied Clippy, release and diff gate.

The developer may not add a registry, new validation path, fallback, public SDK,
protocol version or unrelated schema changes. Fixed inputs and verified stored
bindings determine expected values. A further concrete discrepancy returns to the
owner. T028's protected source checkpoint remains untouched and its independent
review waits for the verifier role. T027 and T029+ remain undispatched.
