# Accept T025 and deliver the bounded T026 integrity checkpoint

Existing Sol-high coordinator; repository `/Users/dowwie/MyProjects/validator`.
Save acknowledgment to `docs/dispatches/validator-t026/001-coordinator-to-owner.response.md`.

## Acceptance and authority

Owner read the complete T025 developer responses, original and focused verdicts,
final Ready021 and coordinator handoff022. Accept the exact final T025 candidate:

| Artifact | SHA-256 |
|---|---|
| `docs/dispatches/validator-t025/020-coordinator-repaired-manifest.md` | `deeed4c908fa385de1cd8494a31fdcc7fb8d46646e03b32766691fe007f24fc9` |
| `docs/dispatches/validator-t025/021-verifier-to-coordinator.response.md` | `a6ca4df6161aea825f5721097a1782b7fc520d5eb3f0febff9f1edb6db9ebc43` |
| `docs/dispatches/validator-t025/022-coordinator-to-owner.response.md` | `8719d7ead5401e2b90b3454128d4e5ff0c34ad8f1f628bb0b7b07ca77d1d86d1` |

The accepted numerical checkpoint includes the independent exhaustive/F04/case
oracles and bounded abstention, legal-class and population-unit corrections.
Preserve the exact staged 17 production plus3 duplicate test Clippy diagnostics;
T027/T035 still require the warning-free literal gates. This is not full product
acceptance. Close Card187 only after recording this owner acceptance.

T026 is now authorized under the existing user build instruction, ratified
specifications, T026 task contract, execution contract and manage-dev-team skill.
T027 and later implementation remain undispatched. Find/update the appropriate
General-board card before creating a duplicate; link the accepted predecessor.

## Outcome and roles

Deliver actual evidence that both task kinds preserve immutable, contained,
replayable artifacts and safe machine outputs under the hostile/late-failure cases
specified by T026. Use a fresh Terra-high/fork-none sole developer for each local
output below, serialized, followed by one fresh Sol-high/fork-none independent
read-only review of the combined frozen candidate. No worker delegates.
Correct T026's historical implementation_role to developer; the independent
verifier does not author permanent tests or production logic.

Read complete `docs/plans/validator/tasks/T026.json`, its governing spec sections
and coverage rows. Reuse current real-API/CLI/owning-module helpers and already
accepted evidence; do not create a generic adversarial framework or duplicate
whole workflows merely to attach another test name. A named case may reuse a
complete existing scenario, but its actual assertions must cover its entire row.

## Ordered local outputs

1. **Artifact and publication safety.** Implement and run
   `artifact_adversarial_matrix`, `publication_and_privacy_matrix`, and exact
   `case_s23` through `case_s28`. Use both task kinds for shared integrity,
   publication and privacy obligations. Complete this coherent output and save
   a full handoff before output2. Confirm no-zero-filter execution, full affected
   targets and format/diff locally.
2. **Source, comparison and preparation evidence.** Fresh developer implements
   exact `case_s15`, `case_s16`, `case_s18`, `case_s19`, `case_s20`, `case_s22`,
   `case_m17`, `case_m18`, `case_m19`, `case_e08`. Complete each stated subcase,
   then run all16 exact T026 cases plus both matrix filters independently, and
   the four literal final commands below. One combined independent review follows.

Do not treat an umbrella pass as proof that a missing exact filter exists. Avoid
repeating full builds at each intermediate step. Return for reassessment after
an incomplete handoff rather than reissuing the same unfinished bundle.

## Required discriminating evidence

- Replay resolves copied evidence only. Same-basename sources, parent-relative
  original paths, repeated paths and UTF-8 source/index ordering remain exact after
  relocation and removal/unavailability of originals. Original provenance strings
  are never dereferenced during replay.
- Corrupt snapshots, digests, stored results and evidence bindings individually:
  missing/extra/duplicate/swapped source-index pairs, wrong original strings,
  wrong ordinal destinations, changed bytes, escaping stored symlinks and report
  source-array disagreement. Inspect and compare reject through typed actual paths;
  no successful comparison directory is published.
- Structural/count/status/recorded-configuration values remain exact. Computed
  result floats alone use the declared replay tolerance, identified by complete
  computed field paths. Exercise a valid within-tolerance metric and invalid
  count/configuration mutation for each task kind; do not widen tolerance or
  accept arbitrary tail-name/dynamic metric discovery.
- Existing file, directory and symlink destinations survive unchanged, including
  concurrent creation at publication. A genuinely late failure leaves no final
  success result. Reuse the existing no-replace publisher and deterministic
  owning-module test seams; no timing-only flaky race or alternate publisher.
- Routine stdout/stderr/errors/reports do not reveal sentinel opaque input or
  arbitrary evidence contents. Explicit selected-episode inspection preserves
  exact JSON including integer9007199254740993. Receipts hash the exact completed
  report/comparison file; run real process exit-category checks.
- Every source/comparison case asserts the normative row: mixed source attribution,
  unknown source rejection, changed definitions despite equal local source IDs,
  changed whole-gold digest, default selection mismatch and explicit intersection,
  all three empty-intersection availability pairings, recovered/regressed IDs,
  multi-label changed nonmatching sets, answered populations/overlap and marginal
  applicability. Reuse independently fixed expected values, never infer expected
  counts from the result being tested.
- E08 preserves original0.99 observation, explicitly prepared canonical vector,
  normalized working values and bound preparation script/raw/receipt evidence as
  distinct artifacts/fields. The rule is external deterministic preparation,
  not an in-process fallback or a claim of calibration.

## Write scope and correction allowance

Primary write scope is `tests/conformance.rs`, `tests/cli.rs` and owning test
sections in `src/artifacts.rs`. Developer may make a smallest required production
correction in the existing `src/artifacts.rs` or `src/app.rs` boundary only after
first reproducing a concrete violation of the above ratified T026 requirement and
preserving the failing input/expected/actual evidence. This permits necessary
integrity/replay/publication fixes, not speculative hardening, architecture changes,
scoring changes, policy changes or new features. Escalate any other owning-layer,
schema, dependency or contract change before writing it. Update task/physical/
downstream mappings for any demonstrated necessary ownership change before dispatch.

Preserve accepted tests/fixtures, all numerical expectations and the T025 repairs.
No private Chord dependency, inference, network service, database/state engine,
framework, warning suppression, fake consumer, public API expansion, placeholder
or fallback. Keep the unborn/untracked workspace intact; no staging/commit/cleanup.
Workers read the Rust skill and owning code before edits. Maintain one writer.

After a complete combined handoff, freeze exact source/schema/test/fixture hashes
and give the verifier T026 only plus material affected regressions. One bounded
finding-driven repair/recheck cycle is authorized within the same scope. A repeated
incomplete handoff, contract dispute or further unresolved defect returns to owner
with cause, exact evidence and smallest next action. Do not turn the independent
review into T027, private acceptance, optional coverage or a new full schema audit.

## Final boundary and resumption state

Run serially on the pinned toolchain using existing targets:

```text
cargo fmt --all -- --check
cargo clippy --all-targets --all-features --locked -- -D warnings
cargo test --all-features --locked
cargo build --release --locked --bin validator
git diff --check
```

Only the exact staged17+3 Clippy diagnostics may remain. All other commands and
every assigned nonzero filter must pass. Preserve precise error codes and exits;
do not replace real process evidence with inferred exit codes when T026 requires
the CLI. The verifier may reuse unchanged full-gate evidence and run the cheapest
bounded additional checks needed for this contract. No gratuitous fresh build tree.

Coordinator owns Fizzy, index and concise accepted-evidence/decision updates,
serialized against any developer documentation. Keep the first incomplete step
accurate, verify rendered notes, index every dispatch/response and accepted fixture,
run verify-plan and diff checks. Preserve task-owned caffeinate PID84732 `-i -m`;
the owner just confirmed live assertions and no new system sleep in macOS logs.
Report milestones, exact active commands and material scope concerns. Return a
complete saved combined handoff and independent verdict for owner acceptance;
T027 does not start automatically.
