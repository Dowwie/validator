# Independently verify the frozen T026 integrity candidate

Role/model: fresh independent verifier, `gpt-5.6-sol`, high reasoning,
`fork_turns: none`. Do not delegate. You did not implement this candidate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #188 — Verify Validator artifact integrity](http://localhost:3006/1/cards/188).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t026/010-verifier-to-coordinator.response.md`.

Save the full substantive Ready/Revise/Blocked verdict there before returning. In
chat return only the path, SHA-256 and verdict. Do not edit source, schemas, tests,
fixtures, governance, plans, index, session notes, acceptance records or Fizzy.
Temporary read-only probes may live outside the repository. Do not delegate.

Reconcile frozen manifest009 SHA-256
`fcb905dcee73958340458b88433e5bd43cc2cc78cfadd37470a19ed50a1efb9f`
and every listed hash before review. Stop Blocked on any mismatch. Read global/
repository AGENTS, manage-dev-team, complete T026, owner prompts001/004/006, both
complete developer responses005/008, governing specification sections and every
T026 coverage row before treating the handoffs as evidence. T027 and T028 source
work are outside this review.

## Independent integrity review

Inspect the complete T026-owned tests and necessary surrounding production code.
Confirm every named filter covers all of its normative subcases through actual
public API or real process paths; wrappers may reuse shared complete workflows but
cannot substitute a name for behavior.

Review the replay matrix for both task kinds:

- actual single/multi valid controls, relocated stored evidence and no original
  path lookup;
- each snapshot/binding/original/ordinal/index/evidence/symlink/source-array/result
  corruption discriminates its intended condition;
- precise inspect and compare code/stage assertions and absent comparison output;
- complete fixed multi-label computed paths only: hard, per-label, macro,
  binary-loss/Brier and marginal-bin values. Independently confirm representative
  inside-tolerance values succeed and outside-tolerance/count/operand/status/
  recorded-configuration mutations reject; no dynamic suffix matching or tolerance
  for arbitrary recorded data;
- unchanged tolerance constants and single-label path behavior.

Review deterministic publication/no-replace/late-failure seams, real CLI exits,
relocation, exact receipts, privacy sentinels and large-integer inspection. Check
both task kinds collectively and distinguish API errors from required process
exit evidence.

Inspect every exact S15/S16/S18-S20/S22-S28, M17-M19 and E08 test against its
ratified row. Reconstruct expected IDs/counts/status pairings from fixed inputs,
not current results. Confirm source definitions/config changes, whole-gold digest
incompatibility, default/intersection behavior and all empty availability pairs;
recovered/regressed IDs; multi-label transition/answered-overlap evidence; and
external preparation bindings with original `.99`, canonical prepared and working
values kept distinct without fallback/calibration claims.

## Executed evidence

Run every exact filter nonzero, both matrices and the accepted replay filter using
the existing target. A short literal-name loop may be used if each exit is
preserved. Confirm 91 conformance and 11 CLI filters list. Then run one full locked
suite and current warning-denied Clippy:

```text
cargo test --all-features --locked
cargo clippy --all-targets --all-features --locked -- -D warnings
```

The suite must pass 44 library, 11 CLI, 91 conformance and zero doc tests. Clippy
may exit 101 only for the accepted exact 17 production plus three duplicate
lib-test staged diagnostics. Reuse unchanged developer format/release/diff evidence
unless a concrete failure justifies rerun. Do not create a fresh target or extend
diagnostics after the scoped verdict is supported.

Return **Ready** only if the exact manifest, two-task integrity/publication
matrices, all 16 cases and bounded gates satisfy T026 with no known blocking
defect. Return **Revise** for each reproducible contract defect with requirement,
location, exact input/expected/actual evidence, consequence and smallest
correction. Return **Blocked** only for a manifest/evidence condition preventing
judgment. Do not request T027 cleanup, private-source verification, optional
hardening, generic audit tooling or duplicate workflows. Stop when the bounded
evidence supports one verdict.
