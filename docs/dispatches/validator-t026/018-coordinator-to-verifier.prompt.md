# Recheck the resolved selected source in T026 inspection

Role/model: same independent verifier from verdicts010/013/addendum015,
`gpt-5.6-sol`, high reasoning, original `fork_turns: none` context. Do not
delegate. You did not implement this candidate.

Repository: `/Users/dowwie/MyProjects/validator`.
Fizzy: [Card #188 — Verify Validator artifact integrity](http://localhost:3006/1/cards/188).

Required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t026/018-verifier-to-coordinator.response.md`.

Save the full substantive Ready/Revise/Blocked verdict before returning only its
path, SHA-256 and verdict. Do not edit repository files or Fizzy. Temporary
read-only probes may live outside the repository. Do not delegate.

Reconcile manifest017 SHA-256
`bb0ea141de203596fcd9ee3c7db8b7bf37f8a2033e7858d05074b9bf03e3cf44` and every listed hash.
Read owner prompt014, addendum015 and complete developer response016. The five
other repaired findings and broad T026 review remain settled. Recheck only R1's
resolved source plus affected schema, relocation, privacy and both-task evidence.

Confirm through code review and focused execution that:

1. inspection resolves exactly one source from the selected raw prediction's
   `source_id` in the already replay-verified stored report source map;
2. the public result retains raw prediction, opaque input and separate evaluation
   configuration, and returns the complete selected source definition only;
3. optional observation definitions, preparation and verified stored evidence
   bindings survive, with no original-path dereference or unrelated source/data;
4. strict inspection schema accepts the real single-label and multi-label results,
   requires the source field, rejects wrong/missing source shapes and preserves
   legal task-specific source kinds;
5. S15 derives complete Q1/Q2 inspected definitions from fixed inputs and retains
   comparison-side definitions; E08/multi-label, relocation and privacy tests
   discriminate the affected requirements.

Run the focused S15/E08/schema/multi-label/relocation/privacy/API or CLI filters
needed to settle those points. Reconcile manifest identity after execution. Reuse
the developer's unchanged full suite, Clippy, release, format, diff and plan
evidence; do not rerun broad gates by habit.

Return **Ready** only if this exact correction closes addendum015 with no known
blocking defect. Return **Revise** for a reproduced current-contract defect with
exact expected/actual evidence and smallest correction. Return **Blocked** only
when identity/evidence prevents judgment. Stop when this bounded recheck supports
a verdict; do not inspect T027/T028 or reopen the five settled repairs.
