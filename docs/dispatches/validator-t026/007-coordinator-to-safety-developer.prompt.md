# Supersede the narrow replay fix with the complete closed multi-label paths

Continue prompt005 as the current sole developer. Do not delegate. Save the same
required complete response:
`/Users/dowwie/MyProjects/validator/docs/dispatches/validator-t026/005-safety-developer-to-coordinator.response.md`.

Read owner prompt006 and acknowledgment006 before completing the production
correction. This file supersedes coordinator prompt006's exact-match-only wording.
The reproduced public failure and reconciled `src/app.rs` ownership remain valid;
do not discard the failing fixture or reset output1 history.

Extend the existing complete-path classifier for the actual closed multi-label
report shapes only:

- raw/final set and aggregate hard-metric scalar `.value` paths;
- raw/final per-label precision, recall and F1 `.value` paths;
- raw/final answered macro-F1's nested metric `.value` path;
- probability per-label and mean binary log-loss/Brier `.value` paths;
- signals per-label marginal-bin `mean_probability` and
  `observed_positive_rate` paths.

Use exact existing serialization keys and closed alternatives. Preserve all
single-label classifiers. A small private helper is permitted if needed for
clarity/complexity, but no dynamic discovery, suffix match, registry, framework or
extension API. Do not change the tolerance or apply it to counts, ratio operands,
population metadata, statuses, arrays, bin boundaries, recorded source/
preparation configuration, observations or arbitrary metric-shaped JSON.

Use the actual multi-label public run with defined hard/probability/bin results.
Through both verified inspect and compare, prove within-tolerance success for a
representative of every distinct new path shape, including raw and final paths.
Prove an outside-tolerance computed value rejects with the precise category and no
comparison output. Retain precise exact negatives for count, operand, status and
recorded/metric-shaped configuration, plus the existing single-label controls.
A fixed test-only path table is sufficient; do not build a cross-product.

Then complete every prompt005 mutation/error/publication dimension, output1's
remaining S23-S28 and CLI matrix reconciliation, and the same 81-conformance/
11-CLI local boundary. Response005 must name the exact added path families,
unchanged tolerance/constants, representative input/stored/rebuilt values,
inspect/compare outcomes and source/test/mapping hashes. Stop before any other
production/schema/dependency change or output2 work.
